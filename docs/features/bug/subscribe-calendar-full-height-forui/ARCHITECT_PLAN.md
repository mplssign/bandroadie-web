# ARCHITECT_PLAN — bug/subscribe-calendar-full-height-forui

## Feature Slug

`bug/subscribe-calendar-full-height-forui`

## Feature Title

Make Subscribe to Calendar bottom sheet full height and use Forui components

## Problem Summary

The Subscribe to Calendar bottom sheet (opened from the Calendar screen and the Calendar tab via the `+ Subscribe to Calendar` link) presents at roughly 56% of the screen height, cutting off content that should be visible without scrolling and leaving conspicuous empty space under the modal barrier. Independent of the height issue, part of the sheet's rendered widget tree bypasses the app's Forui wrappers: the "Copy" button is a hand-built `Material` + `InkWell` and the feed-content toggles ultimately render `Switch.adaptive` rather than the Forui `FSwitch`-backed `AppSwitch`. The sheet should present at full available height and every component inside it should route through the established Forui wrappers.

### Cycle 4 addition — "How to subscribe" typography reads too small

On the PR #281 preview build of Cycles 1–3, Tony reported that the "How to subscribe" block still renders at the 13px caption tier — the calendar-name labels (Apple Calendar, Google Calendar, Outlook), the platform-specific subtext under each label, and the three notes bullets at the bottom of the sheet all use `AppFontSizes.caption` (13px). Tony's explicit sizing requirement:

- Apple Calendar / Google Calendar / Outlook labels: **16px**
- Subtext under those labels (the `File → New Calendar Subscription → Paste link` line and its two siblings): **14px**
- Notes bullets (`Calendar is read-only`, `Updates sync automatically`, `Sync timing varies by app (usually 15min - 24hrs)`): **14px**

This is presentation-only. It does not alter, revert, or reopen the full-height + Forui solution from Cycles 1–3. The Cycle 4 amendment sits underneath the previously-approved implementation; the approved uncommitted src diff — including the two formatting-only hunks around the header `Icon` and `AppProgressIndicator` — must be preserved verbatim.

### Cycle 5 addition — hide URL, rename copy button, make it full width

On the PR #281 preview build of Cycles 1–4, Tony reported (verbatim): _"hide the URL and the label on the 'Copy' button should be 'Copy subscription link' and the button should be full width."_ Three coupled requirements:

1. **Hide the URL.** The subscription URL is currently rendered as monospace text in a bordered `Container` immediately above the feed-content toggles (the `Expanded(Text(url, …))` sibling of `_CopyButton` inside the URL `Row`). Users don't need to see the ICS URL to use the copy affordance — the whole point of a copy button is that the value can stay off-screen. The visible URL surface should go away, while the URL value itself must remain wired into `Clipboard.setData(ClipboardData(text: url))` unchanged.
2. **Rename the button label to `Copy subscription link`.** The default (un-copied) `AppButton.label` inside `_CopyButton.build()` is currently `'Copy'`; that word alone is under-specified once the URL is no longer on screen next to it. The post-tap "copied" label stays `'Copied'` — Tony's feedback did not touch the confirmation state, the snackbar (`'Link copied to clipboard'`) still fires, and pairing a fleeting 2-second `'Copied'` confirmation with the same snackbar is the same confirmation pattern already used by every other copy-to-clipboard flow in the codebase.
3. **Full-width button.** The button should span the available content width of the sheet body (i.e. from `Spacing.pagePadding` on the left to `Spacing.pagePadding` on the right, matching the `SheetFooter` primary button below it). The established idiom is `AppButton.fullWidth: true`, used 23 times across `lib/**` including inside `SheetFooter` itself.

This is presentation + a small trim. It does not alter, revert, or reopen the full-height + Forui solution from Cycles 1–3 or the "How to subscribe" typography hierarchy from Cycle 4. The approved Cycle 1–3 uncommitted src diff (including the two formatting-only hunks around the header `Icon` and `AppProgressIndicator`) and the Cycle 4 typography swaps must both be preserved verbatim underneath Cycle 5.

### Cycle 6 addition — align header + close control with Add Event, move copy button between feed toggles and How-to-subscribe

On the PR #281 preview build of Cycles 1–5, Tony reported (verbatim): _"The header of the Subscribe Calendar sheet should match the Add Event header in terms of styling, font-size and color, positioning, and the close button should match the Add Event drawer close button. Move the 'Copy subscription link' below the list of 'Include in feed' and above 'How to subscribe'."_ Two coupled requirements:

1. **Match the Add Event header.** The reference is [`_buildStickyHeader`](lib/features/events/widgets/event_editor_drawer.dart#L2776-L2870) in `event_editor_drawer.dart`. Its inline title `TextStyle` is `const TextStyle(fontSize: 20, fontWeight: FontWeight.w600, color: Color(0xFFFAFAFA))` (no `fontFamily`, no `height`). The outer wrap is `Padding(EdgeInsets.only(top: 20, bottom: 16))` with horizontal padding supplied by the ancestor scroll padding. The `Row` uses `crossAxisAlignment: CrossAxisAlignment.start`, and its terminal child is `SizedBox(width: 32, height: 32, child: IconButton(padding: EdgeInsets.zero, onPressed: …, icon: const Icon(Icons.close, size: 16, color: Color(0xFFA1A1AA)), style: IconButton.styleFrom(backgroundColor: const Color(0xFF1F1F23), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)))))`. Add Event's `_buildStickyHeader` has _no_ leading icon — the title sits left-anchored. The Subscribe Calendar header currently uses `AppTextStyles.title3` (`Geist / 20 / w600 / height 1.25`) with a leading `Icon(AppIcons.calendar, color: AppColors.primary, size: 22)` + `SizedBox(width: Spacing.space12)`, a default `Row.crossAxisAlignment`, and a close control built from `GestureDetector` → `Container(width: 32, height: 32, decoration: BoxDecoration(color: context.colors.background, shape: BoxShape.circle))` → `Icon(AppIcons.close, size: 18, color: context.colors.textSecondary)`. Header padding is currently split across `SizedBox(height: Spacing.space16)` (above) + `Padding(EdgeInsets.symmetric(horizontal: Spacing.pagePadding))` (row) + `SizedBox(height: Spacing.space16)` (below), giving a 16/16 vertical rhythm that doesn't match Add Event's 20/16.
2. **Move the copy button between the feed toggles and How-to-subscribe.** After Cycle 5, `_CopyButton` sits between the description paragraph and the "Include in feed:" label (as a direct child of the `_buildBody` `Column`, with a `SizedBox(height: Spacing.space20)` on either side). Tony's Cycle 6 target order is: description → space20 → "Include in feed:" label → space8 → five `AppToggleTile`s → space20 → `_CopyButton` → space20 → "How to subscribe:" label → space12 → three `_InstructionTile`s → space20 → notes `Container`. The button's role shifts from an early-body primary action to a mid-body punctuation between "what to include" and "how to hook it up" — appropriate given that the button hands off the link that the how-to-subscribe steps then reference. Spacing tokens (`space20` before and after) are unchanged; only the ordinal position of the `_CopyButton` widget moves.
3. **Title literal — remove the active band name from the header title.** Tony's latest Cycle 6 amendment (verbatim): _"Also don't include the band name in the drawerr title. Title should be only 'Subscribe to Band Calendar'"_. The header title currently interpolates the active band's name via `Text('Subscribe to ${widget.bandName} Calendar', style: AppTextStyles.title3)` at [calendar_subscription_dialog.dart:139](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L139). The Cycle 6 target is the exact static literal `'Subscribe to Band Calendar'` — no `${widget.bandName}` substitution, no other band-derived interpolation, no dynamic content. `widget.bandName` was the sole reader of the dialog's `bandName` field (grep of `widget\.bandName` inside `calendar_subscription_dialog.dart` returns exactly one match — the header title). After the literal swap, the entire `bandName` chain becomes dead — the function parameter at line 27, the intermediate forward at line 37, the widget field at line 44, and the constructor argument at line 49 all thread to a value that no longer has any reader. Both call sites in the calendar feature ([calendar_screen.dart:507-535](lib/features/calendar/calendar_screen.dart#L507-L535), [calendar_tab_content.dart:483-511](lib/features/calendar/calendar_tab_content.dart#L483-L511)) are internal (a grep across `**/*.dart` including `test/**` returns exactly these two call sites plus the definition file — zero external consumers). Cycle 6 therefore removes `bandName` from the dialog's public API (function signature, widget field, constructor argument, intermediate forward) and from both call sites (each caller's `final bandName = ...` local variable and its `bandName: bandName,` named argument), for a total of 8 line deletions across 3 files: 4 lines in `calendar_subscription_dialog.dart` + 2 lines each in `calendar_screen.dart` and `calendar_tab_content.dart`. The removal is a mechanical, module-local expansion of Cycle 6's file scope; retaining `bandName` as an unused required parameter would trip `avoid_unused_constructor_parameters` on the widget field and would confuse future readers who see a value threaded through provider → local → arg → field for no purpose.

This is presentation-only (plus a mechanical dead-parameter cleanup in the same feature module). It does not alter, revert, or reopen the full-height + Forui solution from Cycles 1–3, the "How to subscribe" typography hierarchy from Cycle 4, or the URL-hide + full-width + rename from Cycle 5. Every prior cycle's uncommitted src diff — the `showAppBottomSheet` `mainAxisMaxRatio: 1.0` / `useSafeArea: true` opt-in, the shell `Container` → `DecoratedBox` swap, the `_CopyButton` Forui `AppButton` invocation with `fullWidth: true` and label `'Copy subscription link'`, the `AppToggleTile` Forui `AppSwitch` migration, the four `_InstructionTile` / `_NoteBullet` typography swaps, the URL-container removal, the two formatting-only hunks around the header `Icon` and `AppProgressIndicator` — must be preserved verbatim underneath Cycle 6.

## Root Cause — HIGH confidence (confirmed in code)

Two independent causes, both directly observable in the source.

### 1. Sheet height cap comes from the outer Forui envelope, not the inner container

`showCalendarSubscriptionDialog()` at [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L22-L37) calls `showAppBottomSheet(...)` without passing `mainAxisMaxRatio` or `useSafeArea`:

```dart
showAppBottomSheet(
  context: context,
  isScrollControlled: true,
  backgroundColor: Colors.transparent,
  builder: (context) => CalendarSubscriptionDialog(...),
);
```

The wrapper at [lib/components/ui/app_bottom_sheet.dart](lib/components/ui/app_bottom_sheet.dart#L17-L41) forwards `mainAxisMaxRatio ?? (9 / 16)` to Forui's `showFSheet`, so when the caller omits the argument the outer envelope caps sheet height at `9/16 ≈ 56.25%` of screen height. The dialog's inner shell at [calendar_subscription_dialog.dart:98-104](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L98-L104) then adds a _second_ height cap:

```dart
Container(
  constraints: BoxConstraints(
    maxHeight: (MediaQuery.of(context).size.height - keyboardHeight) * 0.9,
  ),
  ...
```

The inner `0.9` cap is masked by the outer `9/16` envelope — the sheet visibly clamps at `~56%`, never `~90%`. This is the same class of bug diagnosed in [docs/features/detail-sheet-sizing-venue-back/ARCHITECT_PLAN.md](docs/features/detail-sheet-sizing-venue-back/ARCHITECT_PLAN.md#L115-L149) and [docs/features/bug-setlist-picker-drawer-height/ARCHITECT_PLAN.md](docs/features/bug-setlist-picker-drawer-height/ARCHITECT_PLAN.md#L33-L60): the operative cap is the wrapper's default, not any per-sheet inner container. The fix is at the call site, with the inner double-cap removed to leave a single authoritative height ratio.

Sibling reference: [lib/features/calendar/widgets/add_block_out_drawer.dart:83-101](lib/features/calendar/widgets/add_block_out_drawer.dart#L83-L101) passes `mainAxisMaxRatio: 1.0` and `useSafeArea: true` and has no inner max-height container — the target pattern for a full-height calendar sheet.

### 2. Two components in the sheet render non-Forui underneath

The sheet already uses `showAppBottomSheet` (Forui `showFSheet`), `AppProgressIndicator` (Forui `FCircularProgress`), and `SheetFooter` (which delegates to `AppButton` → Forui `FButton`), consistent with the calendar-feature Forui audit at [docs/features/calendar-forui-consolidation/ARCHITECT_PLAN.md](docs/features/calendar-forui-consolidation/ARCHITECT_PLAN.md#L45-L55). Two sub-widgets do not:

**a. `_CopyButton`** at [calendar_subscription_dialog.dart:437-495](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L437-L495) hand-builds a button from `Material(color: ...)` + `InkWell(onTap:...)` + `Padding` + `Row(Icon, Text)`. This bypasses `AppButton` / `FButton` entirely and duplicates styling behaviour (background, radius, splash) that the Forui wrapper already provides.

**b. `AppToggleTile`** at [lib/shared/widgets/toggle_tile.dart:94-100](lib/shared/widgets/toggle_tile.dart#L94-L100) — used exclusively by this sheet for five toggles (`Gigs`, `Potential gigs`, `Rehearsals`, `Potential rehearsals`, `Member block-out days`) — renders `Switch.adaptive` directly rather than delegating to the app's Forui-backed `AppSwitch`. A grep of `lib/**` for `AppToggleTile(` confirms all five current call sites live in this dialog, so the widget is de facto single-use even though it is defined as a shared widget. This is the same class of legacy `Switch.adaptive` → `AppSwitch` migration that [docs/features/unified-forui-switch-styling/ARCHITECT_PLAN.md](docs/features/unified-forui-switch-styling/ARCHITECT_PLAN.md) established as the app-wide standard: `AppSwitch` without an explicit `activeTrackColor`, deferring to the app-theme `FSwitchStyleDelta` defined at [lib/app/theme/app_theme.dart:629-641](lib/app/theme/app_theme.dart#L629-L641).

The remaining hand-built elements in the sheet — the drag-handle stub, the rounded-top shell `Container`, the close-X `GestureDetector` + circle `Container` + `Icon`, and the plain `Text` / `Icon` / `Row` / `Column` atoms — are the documented "house pattern" for Forui-backed sheets in this codebase per [docs/features/calendar-forui-consolidation/ARCHITECT_PLAN.md](docs/features/calendar-forui-consolidation/ARCHITECT_PLAN.md#L51-L55) ("_The house pattern (17 files app-wide) is that each sheet hand-builds its own rounded-container + drag-handle shell because Forui preview's `backgroundColor`/`shape` params are no-ops. This is expected and not a defect._"). They are shared verbatim with `day_detail_bottom_sheet.dart` and `add_block_out_drawer.dart`, so touching them here would break sheet-to-sheet consistency, not restore it.

### 3. Cycle 4 — "How to subscribe" typography uses the 13px caption tier where a 16/14px hierarchy is required — HIGH confidence

Five `AppFontSizes.caption` (13px) references live inside two private helpers in [calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart):

- `_InstructionTile.build()` — the `title` `Text` at [calendar_subscription_dialog.dart:491](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L491) sets `fontSize: AppFontSizes.caption` for the calendar-name labels (Apple Calendar, Google Calendar, Outlook), and the `instruction` `Text` at [calendar_subscription_dialog.dart:499](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L499) sets the same for the platform-specific subtext.
- `_NoteBullet.build()` — the bullet-character `Text` at [calendar_subscription_dialog.dart:527](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L527) and the body `Text` at [calendar_subscription_dialog.dart:534](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L534) both set `fontSize: AppFontSizes.caption` for the three notes at the bottom of the sheet.

Grep confirms both helpers are private and single-use in this file (all `_InstructionTile` / `_NoteBullet` references live inside `calendar_subscription_dialog.dart`), so the change stays fully local. The fifth `AppFontSizes.caption` occurrence in the file — the monospace subscription-URL row at [calendar_subscription_dialog.dart:280](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L280) — is _not_ in Tony's request and must stay at `AppFontSizes.caption`; it is the raw ICS URL string, not narrative content.

The target sizes map exactly to existing app-wide typography tokens defined at [design_tokens.dart:353-355](lib/app/theme/design_tokens.dart#L353-L355): `AppFontSizes.body = 16.0` and `AppFontSizes.subhead = 14.0`. A prior grep across `lib/**` returned ~200 call sites across 60 files for these two tokens (`app_theme.dart`, `login_screen.dart`, `calendar_event_card.dart`, `home_app_bar.dart`, `financials_screen.dart`, and many more) — they are the canonical 16/14 tokens in the codebase. No new token is required, and no design-token file is touched.

### 4. Cycle 5 — private-info surface visible, button label under-specified, button not full-width — HIGH confidence (confirmed in code)

Three coupled defects live in the URL row and the copy affordance inside `_buildBody`:

- **URL rendered on screen.** [calendar_subscription_dialog.dart:275-291](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L275-L291) renders the ICS URL as `Expanded(Text(url, style: TextStyle(color: context.colors.textSecondary, fontSize: AppFontSizes.caption, fontFamily: 'monospace'), maxLines: 2, overflow: TextOverflow.ellipsis))` inside a bordered `Container`. This visibly leaks the private token-carrying URL to anyone looking at the screen (over-the-shoulder, screen share, screenshot). Tony's Cycle 5 feedback removes this surface entirely; the URL string only ever needs to reach the system clipboard, not the user's eyeballs.
- **`_CopyButton.build()` label reads `'Copy'`.** [calendar_subscription_dialog.dart:449-460](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L449-L460) sets `label: copied ? 'Copied' : 'Copy'`. With the URL text gone, the un-copied label loses its adjacent visual anchor and reads as under-specified — `'Copy subscription link'` is the exact string Tony specified and is what the button should say by default.
- **`_CopyButton` renders at its intrinsic content width.** The `AppButton` invocation at [calendar_subscription_dialog.dart:449-460](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L449-L460) omits `fullWidth:`, defaulting to `false` per [app_button.dart:39](lib/components/ui/app_button.dart#L39). Inside the URL `Row` this rendered as a small right-aligned button; once the URL text is removed, the natural fill for a primary action inside a sheet body is `fullWidth: true`, matching every primary-action button pattern in `SheetFooter`, `login_screen.dart`, `notification_settings_modal.dart`, `set_break_screen.dart`, and 19 other `AppButton.fullWidth: true` call sites in the codebase.

All three are HIGH confidence — the code is on the branch at `3ffd066` and the defect surface is literal-string / literal-widget-tree. No hypothesis required.

The Cycle 4 verification carve-out that requires the URL row's `AppFontSizes.caption` reference at line 280 to remain untouched is superseded by Cycle 5: removing the entire URL `Container` (comment, decoration, and the inner `Row` with `Expanded(Text(url, …))` + `SizedBox` + `_CopyButton`) takes the final `AppFontSizes.caption` reference in the file with it. That is a clean side effect, not a Cycle 4 regression — the token itself and its ~40 other call sites across `lib/**` are untouched.

### 5. Cycle 6 — header styling drifts from Add Event; copy button sits above feed toggles instead of between them — HIGH confidence (confirmed in code)

Two independent defects, both directly observable in the current branch source.

**a. Header drift.** The Subscribe Calendar header at [calendar_subscription_dialog.dart:117-159](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L117-L159) differs from the Add Event `_buildStickyHeader` reference at [event_editor_drawer.dart:2776-2870](lib/features/events/widgets/event_editor_drawer.dart#L2776-L2870) on five discrete axes:

- **Title style.** Subscribe uses `AppTextStyles.title3` — `Geist / 20 / w600 / height 1.25` with color inherited from ambient `DefaultTextStyle`. Add Event uses inline `const TextStyle(fontSize: 20, fontWeight: FontWeight.w600, color: Color(0xFFFAFAFA))` — no `fontFamily` (inherits Geist from the ambient theme, same result), no `height` (uses font-metric default vs. 25px line box). The rendered font family, size, weight, and pixel color match in dark mode; the line-box height differs by ~5px which nudges the title's vertical rest position when the `Row` uses `crossAxisAlignment.start`.
- **Leading icon.** Subscribe places `Icon(AppIcons.calendar, color: AppColors.primary, size: 22)` + `SizedBox(width: Spacing.space12)` as the first row child, shifting the title ~34px right of the left content edge. Add Event has no leading icon — the title `Expanded` is the first row child and the title sits at the left content edge.
- **Row cross-axis alignment.** Subscribe uses the default (`CrossAxisAlignment.center`). Add Event uses explicit `CrossAxisAlignment.start`.
- **Header vertical padding.** Subscribe surrounds the header row with `SizedBox(height: Spacing.space16)` above (post-drag-handle) and `SizedBox(height: Spacing.space16)` below (pre-scroll-view) — a 16/16 rhythm. Add Event wraps its row in `Padding(EdgeInsets.only(top: 20, bottom: 16))` — a 20/16 rhythm.
- **Close control widget tree.** Subscribe builds the close-X from `GestureDetector(onTap: pop) → Container(width: 32, height: 32, decoration: BoxDecoration(color: context.colors.background /* == 0xFF09090B in dark */, shape: BoxShape.circle)) → Icon(AppIcons.close /* == LucideIcons.x */, size: 18, color: context.colors.textSecondary /* == 0xFFA1A1AA in dark */)`. Add Event builds it from `SizedBox(width: 32, height: 32, child: IconButton(padding: EdgeInsets.zero, onPressed: pop, icon: const Icon(Icons.close /* Material glyph */, size: 16, color: Color(0xFFA1A1AA)), style: IconButton.styleFrom(backgroundColor: const Color(0xFF1F1F23), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)))))`. The differences are: (i) `GestureDetector` vs. `IconButton` (Material `IconButton` adds button-role semantics, focus ring, splash, and keyboard activation that `GestureDetector` does not — an accessibility upgrade); (ii) circle vs. 8px-radius rounded square shape; (iii) `context.colors.background` (near-black `0xFF09090B`) vs. `Color(0xFF1F1F23)` (a slightly-lighter charcoal that does not map to any `BrandColors` token but is the established `kEdCardBorder` value from [event_editor_theme.dart:9](lib/app/theme/event_editor_theme.dart#L9) and is repeated as an inline literal at three other `IconButton.styleFrom(backgroundColor: ...)` sites in the app — [venue_detail_screen.dart:131](lib/features/contacts/widgets/venue_detail_screen.dart#L131), [add_financial_entry_bottom_sheet.dart:699](lib/features/financials/widgets/add_financial_entry_bottom_sheet.dart#L699), [view_gig_drawer.dart:386](lib/features/gigs/widgets/view_gig_drawer.dart#L386)); (iv) 18px `LucideIcons.x` vs. 16px Material `Icons.close` glyph.

Tony's cycle-6 target is byte-exact parity with Add Event on all five axes. He explicitly framed this as a visual-parity requirement, not a Forui-consolidation requirement — see the Cycle 6 addition subsection in Problem Summary above.

**b. Copy-button placement drift.** After Cycle 5, the widget-tree order inside `_buildBody` (calendar_subscription_dialog.dart:245-397 as of the branch's uncommitted state) reads: description `Text` → `SizedBox(height: Spacing.space20)` → `_CopyButton(...)` → `SizedBox(height: Spacing.space20)` → "Include in feed:" label → `SizedBox(height: Spacing.space8)` → five `AppToggleTile`s (with `SizedBox(height: Spacing.space4)` between) → `SizedBox(height: Spacing.space20)` → "How to subscribe:" label → `SizedBox(height: Spacing.space12)` → three `_InstructionTile`s (with `SizedBox(height: Spacing.space8)` between) → `SizedBox(height: Spacing.space20)` → notes `Container`. Tony's Cycle 6 target inserts the button _after_ the toggles and _before_ "How to subscribe:", using the two existing `SizedBox(height: Spacing.space20)` gaps as its bracket. The button widget itself and every one of its arguments (`url`, `copied`, `onCopied` closure with `setState` + snackbar + delayed reset) stay verbatim — only its ordinal position in the `Column` children list moves.

**c. Title interpolates active band name; `bandName` parameter is dead after removal — HIGH confidence.** The Subscribe Calendar header title at [calendar_subscription_dialog.dart:139](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L139) currently renders `Text('Subscribe to ${widget.bandName} Calendar', style: AppTextStyles.title3)` — active-band name interpolated inline. Tony's latest Cycle 6 amendment sets the target to the exact static literal `'Subscribe to Band Calendar'` with no interpolation and no band-derived content of any kind. Grep of `calendar_subscription_dialog.dart` for `bandName|widget\.` returns exactly eight occurrences: the function parameter `required String bandName,` at line 27, the forwarded call `bandName: bandName,` at line 37, the widget field `final String bandName;` at line 44, the constructor argument `required this.bandName,` at line 49, four `widget.bandId` reads at lines 74/87/98/98 (feeding the service, provider, and preferences updates), and exactly one `widget.bandName` read at line 139 (the header title). After the literal swap eliminates the sole `widget.bandName` reader, the entire `bandName` chain from function parameter → forward → field → constructor becomes dead — a required parameter threaded through provider → local → arg → field with no downstream consumer. Grep of `**/*.dart` across the entire workspace (including `test/**`, `bandroadie_fresh/**`, `marketing/**`, and every other Dart source root) for `showCalendarSubscriptionDialog(|CalendarSubscriptionDialog(` returns exactly the three sites already listed in the Existing System Analysis (the definition file and two internal call sites) — zero external consumers, zero test callers, zero plugin callers. Both call sites in [calendar_screen.dart:507-535](lib/features/calendar/calendar_screen.dart#L507-L535) and [calendar_tab_content.dart:483-511](lib/features/calendar/calendar_tab_content.dart#L483-L511) compute `bandName` locally with an `?? 'Band'` soft fallback — that fallback already conceded the value is optional at the data layer, so users who trigger the sheet when `activeBand` is null already see `'Subscribe to Band Calendar'`. Tony's Cycle 6 amendment makes that the always case. The dead parameter is safe to remove across the entire API chain in the same minimal cycle: function signature, widget field, constructor arg, intermediate forward call, and both call-site invocations plus their two local `final bandName = ...` declarations. Net: 4 line deletions in `calendar_subscription_dialog.dart`, 2 line deletions each in `calendar_screen.dart` and `calendar_tab_content.dart` — 8 lines total across 3 files.

All three defects are HIGH confidence — the reference file, the current file, the target visual, and the call-site audit are all in the tree at concrete line ranges. No hypothesis required.

## Existing System Analysis

- The sheet is opened from exactly two sites: [lib/features/calendar/calendar_screen.dart:507-535](lib/features/calendar/calendar_screen.dart#L507-L535) and [lib/features/calendar/calendar_tab_content.dart:483-511](lib/features/calendar/calendar_tab_content.dart#L483-L511). Both call `showCalendarSubscriptionDialog(context, ref, bandId: ..., bandName: ...)`. Neither site controls sheet chrome — the chrome is entirely owned by the dialog and its `showAppBottomSheet` wrapper call.
- `showAppBottomSheet` already supports the two params required for the height fix (`mainAxisMaxRatio`, `useSafeArea`) with backward-compatible defaults — the `?? (9 / 16)` fallback for `mainAxisMaxRatio` and the forwarded `useSafeArea` were both added and validated in the `detail-sheet-sizing-venue-back` cycle. No wrapper change is required here.
- `_CopyButton` in this file has one caller (its parent `_buildBody`). It is a private `StatelessWidget` — replacing its implementation in place preserves the public shape of the file and every state-management path unchanged: `_copied` still lives on `_CalendarSubscriptionDialogState`, the `onCopied` callback still runs `setState(() => _copied = true)` followed by a delayed reset, and the `Clipboard.setData` call still fires on tap.
- `AppButton` (Forui `FButton`) at [lib/components/ui/app_button.dart:100-206](lib/components/ui/app_button.dart#L100-L206) already accepts a leading `icon`, an optional `backgroundColor` override that is applied through a `FButtonStyleDelta.decoration` delta (the docstring's "ignored in Forui preview" comment is stale — the code applies it), and an `isLoading` spinner. It is a drop-in for the `_CopyButton`'s copy/copied dual state.
- `AppSwitch` at [lib/components/ui/app_switch.dart:47-76](lib/components/ui/app_switch.dart#L47-L76) accepts `value`, `onChanged`, and (optionally) `activeTrackColor` / `activeColor`. The current app-wide convention set by `unified-forui-switch-styling` is to _omit_ the explicit `activeTrackColor` on `AppSwitch` call sites and let the theme delta handle the selected-track color (`AppColors.primarySoft`) and the off-track color (`AppColors.switchTrackOff`). `AppToggleTile` is the only widget in `lib/` still using `Switch.adaptive` after that convention was adopted, and its `inactiveTrackColor` / `inactiveThumbColor` overrides duplicate what the theme already sets.
- `AppToggleTile`'s public API (`title`, `subtitle`, `value`, `onChanged`, `enabled`, `compact`) is preserved by an internal `AppSwitch` swap. No caller-visible change; the shared-widget abstraction stays intact for any future caller.
- The sheet's keyboard-inset handling at [calendar_subscription_dialog.dart:92-93](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L92-L93) reads `MediaQuery.of(context).viewInsets.bottom` and `MediaQuery.of(context).padding.bottom` and forwards `safeBottom` into the scroll view's bottom padding at line 178. That handling remains correct once the outer envelope is `mainAxisMaxRatio: 1.0` and `useSafeArea: true` — Forui's `showFSheet` continues to expose `viewInsets` through the descendant `MediaQuery` per the same pattern proven in `add_block_out_drawer.dart`. The `MediaQuery.of` → `viewInsetsOf` / `viewPaddingOf` narrowing suggested in [docs/features/bulk-entry-instructions-cutoff-ios/ARCHITECT_PLAN.md](docs/features/bulk-entry-instructions-cutoff-ios/ARCHITECT_PLAN.md) is a separate systemic concern and is not in scope here.
- Cycle 4: `_InstructionTile` and `_NoteBullet` are private, file-local widgets. Grep across `lib/**` finds every reference inside `calendar_subscription_dialog.dart`; nothing outside this file consumes either. No widget test in `test/**` references `calendar_subscription_dialog` (a grep of `test/**` returned zero matches), so there is no golden or widget test to update alongside the typography swap.
- Cycle 5: The URL `Container` at [calendar_subscription_dialog.dart:271-311](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L271-L311) is a self-contained block — `Container` → `Row` → `Expanded(Text(url, …))` + `SizedBox(width: Spacing.space12)` + `_CopyButton(url: url, copied: _copied, onCopied: …)`. It sits between the description `Text` (opening paragraph) and the `SizedBox(height: Spacing.space20)` that precedes "Include in feed:". Removing the `Container` + inner `Row` scaffolding — and lifting `_CopyButton(...)` up to be a direct child of the `_buildBody` `Column` — is a mechanical simplification: no other widget in `_buildBody` reads from that container, no state depends on the URL text being present in the tree, and the `_CopyButton` already receives `url` as a constructor param.
- Cycle 5: `_CopyButton` is a private `StatelessWidget` with three fields (`url`, `copied`, `onCopied`) and one call site (inside `_buildBody`). Its `AppButton(...)` invocation already applies `label`, `icon`, `backgroundColor`, and `onPressed`; the only Cycle 5 delta inside the widget is a swap of the un-copied label literal (`'Copy'` → `'Copy subscription link'`) and a `fullWidth: true` addition. The `AppButton` wrapper's `fullWidth: true` implementation at [app_button.dart:161-164](lib/components/ui/app_button.dart#L161-L164) is `SizedBox(width: double.infinity, child: button)` — a purely additive wrapper that composes with the rest of the wrapper's style logic and is used 23 times elsewhere in the app.
- Cycle 5: `_CopyButton`'s `url` field still flows into `Clipboard.setData(ClipboardData(text: url))` inside its `onPressed` closure — the copy target and its data source are unaffected. The `_copied` state on `_CalendarSubscriptionDialogState`, the 2-second delayed reset in `_buildBody`'s `onCopied` closure, the `showSuccessSnackBar(context, message: 'Link copied to clipboard')` call, and the icon/background color transitions all stay verbatim. The `calendarBandSubscriptionUrlProvider(widget.bandId)` `AsyncValue.when(data: …, loading: …, error: …)` guard at [calendar_subscription_dialog.dart:172-175](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L172-L175) already prevents `_buildBody` (and therefore `_CopyButton`) from ever being reached with a null / erroring URL — no new loading or error state is introduced.
- Cycle 5: Grep of `calendar_subscription_dialog.dart` confirms the only reader of the URL string inside the sheet body is that one `Expanded(Text(url, …))` — no accessibility helper, semantic label, or diagnostic overlay depends on it being present. Removing it leaves the button with a clear, self-descriptive label (`'Copy subscription link'`) that improves rather than reduces screen-reader clarity.
- Cycle 6: The reference [`_buildStickyHeader`](lib/features/events/widgets/event_editor_drawer.dart#L2776-L2870) lives inside `_buildScrollableBody` and is composed as a plain `Column` child (not a `SliverPersistentHeader`) — the "sticky" in its name is a legacy misnomer; it scrolls with the body. The Add Event drawer wraps its scroll view in `SingleChildScrollView(padding: EdgeInsets.all(16), ...)` at [event_editor_drawer.dart:2733-2735](lib/features/events/widgets/event_editor_drawer.dart#L2733-L2735), so the header's `Padding(EdgeInsets.only(top: 20, bottom: 16))` sits on top of a 16px horizontal ancestor padding. The subscribe sheet already applies horizontal padding via `Padding(EdgeInsets.symmetric(horizontal: Spacing.pagePadding))` (== 16) — the `top: 20, bottom: 16` verticals can be added by merging the wrapping padding to `EdgeInsets.fromLTRB(Spacing.pagePadding, 20, Spacing.pagePadding, 16)` and dropping the two `SizedBox(height: Spacing.space16)` spacers that currently sandwich the header. This preserves the sheet's shell layout (drag handle → header outside scroll → `Flexible` scroll → `SheetFooter`) — the header does not need to move inside the scroll body to produce the target visual. Moving it inside the scroll would break the `Flexible` boundary that keeps `SheetFooter` sticky and let the header scroll away with the body — an unintended side effect of a purely visual ask.
- Cycle 6: The drag handle above the header (`Container(margin: EdgeInsets.only(top: 12), width: 40, height: 4, decoration: BoxDecoration(color: context.colors.border, borderRadius: BorderRadius.circular(2)))` at [calendar_subscription_dialog.dart:107-115](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L107-L115)) is the documented Forui-sheet house pattern per [docs/features/calendar-forui-consolidation/ARCHITECT_PLAN.md](docs/features/calendar-forui-consolidation/ARCHITECT_PLAN.md#L51-L55) and is present verbatim on `add_block_out_drawer.dart` and `day_detail_bottom_sheet.dart`. Add Event's drawer opens via `showModalBottomSheet` (Material) and has no drag handle — a different top-level presentation. Removing the drag handle from Subscribe Calendar to fully mirror Add Event's chrome would (i) drift this sheet away from its two Forui-sheet siblings and (ii) exceed Tony's Cycle 6 ask, which targets the header row and close control specifically. Drag handle stays; the vertical distance from drag-handle bottom to title top after Cycle 6 becomes `Spacing.space4` (drag-handle height's trailing implicit gap absent — the row's own `top: 20` is measured from the drag-handle margin box) ≈ 20px — comfortable and readable.
- Cycle 6: `IconButton` is a Material widget, not a Forui widget. The Forui `FButton.icon` primitive wraps children in FButton chrome (background from `FButtonStyleDelta`, radius from theme, hover/press decoration) — it does not produce the exact 32×32 pill with `Color(0xFF1F1F23)` background and 8px corner radius that Add Event uses. Substituting `FButton.icon` here would break byte-exact parity with Add Event, which is the explicit Cycle 6 goal. Additionally, `IconButton` provides button-role semantics (screen readers announce "Close, button"), focus/keyboard activation (Enter/Space triggers `onPressed`), and material ink splash — all of which the current `GestureDetector` lacks. The Cycle 6 swap therefore improves accessibility while achieving the requested visual match. The scope note in the file's title ("full-height-forui") applied to the earlier `_CopyButton` and `AppToggleTile` migrations, which had `AppButton` / `AppSwitch` wrappers ready to consume; no equivalent Forui wrapper exists for the Add Event close-pill geometry, so the correct move is to use the same `IconButton` primitive Add Event uses. This is not a Forui regression — the sheet's other Forui migrations (`_CopyButton` → `AppButton`, `AppToggleTile` → `AppSwitch`, `showAppBottomSheet` → `showFSheet`) are untouched.
- Cycle 6: The `Color(0xFF1F1F23)` close-button background and `Color(0xFFA1A1AA)` icon color are the same literal hex codes used at [event_editor_drawer.dart:2842-2848](lib/features/events/widgets/event_editor_drawer.dart#L2842-L2848) and repeated at [venue_detail_screen.dart:131](lib/features/contacts/widgets/venue_detail_screen.dart#L131), [add_financial_entry_bottom_sheet.dart:699](lib/features/financials/widgets/add_financial_entry_bottom_sheet.dart#L699), and [view_gig_drawer.dart:386](lib/features/gigs/widgets/view_gig_drawer.dart#L386). `Color(0xFFA1A1AA)` == `context.colors.textSecondary` in dark mode; `Color(0xFF1F1F23)` == `kEdCardBorder` from [event_editor_theme.dart:9](lib/app/theme/event_editor_theme.dart#L9) but has no direct `BrandColors` token. Hardcoding both hex codes inline (instead of routing through `context.colors.textSecondary` and a new token) is the pattern Add Event and the three other sites already use — and it matches Tony's exact ask verbatim. The app is dark-mode-only per copilot instructions ("Dark mode only, rose accent #F43F5E"), so hardcoded hex and `context.colors.textSecondary` resolve to the same pixels; the literal-vs-token choice is a code-readability decision, not a runtime one. Choosing the literal for the Cycle 6 close button minimizes drift from Add Event on `git blame` and keeps the four production `IconButton.styleFrom(backgroundColor: Color(0xFF1F1F23))` sites textually identical for future refactor.
- Cycle 6: The title text style change from `AppTextStyles.title3` to inline `const TextStyle(fontSize: 20, fontWeight: FontWeight.w600, color: Color(0xFFFAFAFA))` is byte-equivalent in dark mode (Geist family inherited from ambient theme, same 20px size, same w600 weight, same textPrimary color) except for one subtle field: `AppTextStyles.title3` sets `height: 1.25` (25px line box) while Add Event's inline style omits `height` (font-metric default). For a single-line title inside a `Row` with `CrossAxisAlignment.start`, the `height` difference shifts the baseline by ~2.5px. Byte-exact parity requires matching Add Event's inline style verbatim — with the same explicit hex color `Color(0xFFFAFAFA)` — so the plan specifies the inline literal, not the token.
- Cycle 6: The leading `Icon(AppIcons.calendar, ...)` currently in the Subscribe Calendar header carries no functional payload — it is decorative visual identity. Removing it to match Add Event's left-anchored title (which has no leading icon) causes no data or navigation regression. The description paragraph at the top of `_buildBody` ("Add your BandRoadie events to your favorite calendar app…") already establishes the sheet's calendar context; the title itself ("Subscribe to `${widget.bandName}` Calendar") also contains the word "Calendar". No user-comprehension regression from dropping the leading icon.
- Cycle 6: Grep of `calendar_subscription_dialog.dart` after Cycle 5 confirms `_CopyButton(...)` appears exactly once in `_buildBody` — as a direct child of the outer `Column`, bracketed by two `SizedBox(height: Spacing.space20)` gaps. Moving it means: cut the `_CopyButton(...)` node from between the description paragraph and the "Include in feed:" label, paste it between the last `AppToggleTile` (or its trailing `SizedBox(height: Spacing.space4)`, if that remains) and the "How to subscribe:" label; the two surrounding `SizedBox(height: Spacing.space20)` gaps continue to bracket the button in its new position (the `space20` gap between the toggles block and "How to subscribe:" is currently a single `SizedBox`; after Cycle 6 it becomes two `SizedBox(height: Spacing.space20)` sandwiching the button, giving the same 20/20 rhythm the button had in its old position). Net widget count is unchanged.
- Cycle 6: Grep of `calendar_subscription_dialog.dart` for `widget.bandName` returns exactly one occurrence — the header title's `Text('Subscribe to ${widget.bandName} Calendar', style: AppTextStyles.title3)` at line 139. After Cycle 6's title literal swap, `widget.bandName` has zero remaining readers in the file. The full `bandName` chain — function parameter at line 27 → forwarded call at line 37 → widget field at line 44 → constructor argument at line 49 → the deleted `widget.bandName` read — becomes fully dead. Removing all four lines from the dialog file plus the corresponding argument in the forwarded `CalendarSubscriptionDialog(...)` call is mechanical and does not affect any other identifier or symbol. Grep across `lib/**` for `showCalendarSubscriptionDialog(` returns exactly two call sites — [calendar_screen.dart:516](lib/features/calendar/calendar_screen.dart#L516) and [calendar_tab_content.dart:492](lib/features/calendar/calendar_tab_content.dart#L492) — both internal to the calendar feature. Grep across the entire workspace (`**/*.dart`) for `CalendarSubscriptionDialog|showCalendarSubscriptionDialog` returns exactly the three sites (definition + two callers) — zero external consumers, zero test callers, zero plugin callers, zero references from the marketing app or `bandroadie_fresh/` scratch tree. Each caller opens the sheet from a `GestureDetector` `onTap` closure that reads `activeBand` from `activeBandProvider`, computes a local `final bandName = activeBand?.name ?? 'Band';` (or `bandState.activeBand?.name ?? 'Band';` in the tab-content variant), and passes it to `showCalendarSubscriptionDialog(...)`. Nothing else in either closure reads the local `bandName`, so removing the parameter at the receiving side also removes the local variable at each caller (2 lines per site: the `final bandName = …;` declaration and the `bandName: bandName,` named argument in the call). Total 8-line deletion across 3 files: 4 lines in `calendar_subscription_dialog.dart` (function param, forward, widget field, constructor arg) + 2 lines each in `calendar_screen.dart` and `calendar_tab_content.dart`. The removal is safe as a plan-listed expansion of Cycle 6's file scope; both callers are already read in the Existing System Analysis and their surrounding widget trees are unaffected.
- Cycle 6: Retaining `bandName` as an unused required parameter (as a "compatibility" hedge) is a false conservative — it would trip `avoid_unused_constructor_parameters` on `CalendarSubscriptionDialog.bandName` (an analyzer warning; not a mere info-level lint) and would leave a misleading value threaded through the API for no purpose. Renaming to `_bandName` to silence the analyzer is worse — it hides the deadness rather than removing it. Defaulting the parameter (`String bandName = ''`) at the receiving end is worse still — it lets the caller pass anything and the value never lands anywhere. The correct move for a dead required parameter with zero external consumers is deletion, not preservation.

## Proposed Solution

Five coordinated edits across two files (Cycles 1–3 land the height + Forui migration in `calendar_subscription_dialog.dart` and `toggle_tile.dart`; Cycle 4 lands a font-size hierarchy correction inside `calendar_subscription_dialog.dart` alone; Cycle 5 lands URL-hide + button-rename + full-width inside `calendar_subscription_dialog.dart` alone; Cycle 6 lands header/close-button parity with Add Event + body-order reshuffle inside `calendar_subscription_dialog.dart` alone). The Verification Plan section below states how each edit is validated.

### 1. Height — pass `mainAxisMaxRatio: 1.0` and `useSafeArea: true`, drop the redundant inner cap

- In `showCalendarSubscriptionDialog()`, add `mainAxisMaxRatio: 1.0` and `useSafeArea: true` to the existing `showAppBottomSheet(...)` call. The outer envelope now correctly expresses "full available height".
- In `_CalendarSubscriptionDialogState.build()`, replace the shell `Container` with a `DecoratedBox`, preserving its `decoration:` (rounded-top surface) and `child:` verbatim; the inner `constraints:` is dropped as part of the swap. Because the shell no longer holds any layout arguments (constraints/padding/margin/etc.), keeping it as a `Container` trips the `use_decorated_box` lint; `DecoratedBox` is the analyzer-clean replacement that preserves visual behaviour byte-for-byte. The outer envelope is now the single authoritative height cap — the same one-authoritative-cap pattern established for the setlist picker in `bug-setlist-picker-drawer-height` and applied consistently across `add_block_out_drawer.dart`, `event_editor_drawer.dart`, and other full-height sheets.
- Remove the now-unused `final keyboardHeight = MediaQuery.of(context).viewInsets.bottom;` local at [calendar_subscription_dialog.dart:92](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L92). It was consumed only by the inner cap; a re-read of `_buildBody` / the scroll view confirms that after the cap is removed, the sole remaining consumer of the keyboard-inset locals is the scroll view's bottom padding at line 178, which uses `safeBottom` alone. Keeping `keyboardHeight` around trips `unused_local_variable`. `safeBottom` stays as-is — the keyboard handling already works and is orthogonal to the outer envelope.

### 2. Forui — replace `_CopyButton` internals with `AppButton`

- Replace the `Material` + `InkWell` + `Padding` + `Row` tree in `_CopyButton.build()` with a single `AppButton` call:
  - `label` — `copied ? 'Copied' : 'Copy'`
  - `icon` — `copied ? AppIcons.check : AppIcons.copy`
  - `backgroundColor` — `copied ? context.colors.success : null` (preserves the rose → green state transition; when `null`, `AppButton` falls back to the theme's primary rose)
  - `onPressed` — the existing tap handler: `Clipboard.setData(ClipboardData(text: url)); onCopied();`
- Do _not_ pass `variant:`. `AppButton`'s default is `AppButtonVariant.primary` ([app_button.dart:34](lib/components/ui/app_button.dart#L34)), so an explicit `variant: AppButtonVariant.primary` trips `avoid_redundant_argument_values`. Omitting the argument gives the same behaviour with a clean analyzer.
- The public shape of `_CopyButton` (private widget, three required fields: `url`, `copied`, `onCopied`) is preserved. The parent `_buildBody` continues to construct it identically.
- No change to the `_copied` state machine on `_CalendarSubscriptionDialogState`, the snackbar (`showSuccessSnackBar`), the 2-second delayed reset, or the `Clipboard.setData` payload.

### 3. Forui — migrate `AppToggleTile` internals from `Switch.adaptive` to `AppSwitch`

- In `AppToggleTile.build()`, replace `Switch.adaptive(value: ..., onChanged: ..., activeTrackColor: AppColors.primary, inactiveTrackColor: context.colors.surfaceOverlay, inactiveThumbColor: context.colors.textSecondary)` with `AppSwitch(value: value, onChanged: enabled ? onChanged : null)`.
- Drop the `activeTrackColor` / `inactiveTrackColor` / `inactiveThumbColor` explicit overrides — the app theme `FSwitchStyleDelta` at `app_theme.dart:629-641` provides `AppColors.switchTrackOff` (off), `AppColors.primarySoft` (selected), and `Colors.white` (thumb, all states). This aligns `AppToggleTile` with every other `AppSwitch` call site in the app after the `unified-forui-switch-styling` cycle.
- Public API of `AppToggleTile` is unchanged: `title`, `subtitle`, `value`, `onChanged`, `enabled`, `compact` stay in place. Every existing call site (all five in `calendar_subscription_dialog.dart`) continues to compile and render.
- Update the imports in `toggle_tile.dart` to add `AppSwitch` and drop `brand_colors.dart` / `AppColors` if no other reference remains after the swap (verify per-line before removing).

### 4. Cycle 4 — Swap the five `AppFontSizes.caption` uses inside `_InstructionTile` and `_NoteBullet` to `AppFontSizes.body` / `AppFontSizes.subhead`

- In `_InstructionTile.build()` at [calendar_subscription_dialog.dart:491](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L491), change the title `Text`'s `fontSize` from `AppFontSizes.caption` to `AppFontSizes.body` (13 → 16). Leave `color: context.colors.textPrimary`, `fontWeight: FontWeight.w500`, and the surrounding `Text` / `Column` / `Row` structure exactly as-is.
- In `_InstructionTile.build()` at [calendar_subscription_dialog.dart:499](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L499), change the instruction `Text`'s `fontSize` from `AppFontSizes.caption` to `AppFontSizes.subhead` (13 → 14). Leave `color: context.colors.textSecondary` and every other `TextStyle` field unchanged.
- In `_NoteBullet.build()` at [calendar_subscription_dialog.dart:527](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L527), change the bullet-character `Text('•  ')`'s `fontSize` from `AppFontSizes.caption` to `AppFontSizes.subhead`. Leave `color: context.colors.textMuted` unchanged.
- In `_NoteBullet.build()` at [calendar_subscription_dialog.dart:534](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L534), change the body `Text(text)`'s `fontSize` from `AppFontSizes.caption` to `AppFontSizes.subhead`. Leave `color: context.colors.textMuted` unchanged.
- Do _not_ touch the fifth `AppFontSizes.caption` in the file, at [calendar_subscription_dialog.dart:280](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L280) — that is the monospace subscription-URL row and is out of scope for this feedback.
- Do _not_ introduce hardcoded numeric literals (`fontSize: 16`, `fontSize: 14`). Route through `AppFontSizes.body` and `AppFontSizes.subhead`, matching the ~200 existing call sites of those tokens across `lib/**`.
- Do _not_ change the `_InstructionTile` constructor, its fields (`icon`, `title`, `instruction`), its `Icon` size or color, its `Column` alignment, or its `Row` padding. Do _not_ change the `_NoteBullet` constructor, its field (`text`), the surrounding notes `Container` decoration, or any `SizedBox` spacing.
- Do _not_ touch `toggle_tile.dart` — Cycle 3's Forui swap remains APPROVED and unchanged.

### 5. Cycle 5 — Hide the URL, rename the copy label, make the button full-width

- In `_buildBody(context, url)` inside `_CalendarSubscriptionDialogState`, remove the entire URL surface — the `// Subscription URL` comment, the wrapping `Container` (with its `padding: const EdgeInsets.all(Spacing.space12)` and its `decoration: BoxDecoration(color: context.colors.surfaceElevated, borderRadius: BorderRadius.circular(Spacing.buttonRadius), border: Border.all(color: context.colors.border))`), the inner `Row` with its `children: [ Expanded(Text(url, style: TextStyle(color: context.colors.textSecondary, fontSize: AppFontSizes.caption, fontFamily: 'monospace'), maxLines: 2, overflow: TextOverflow.ellipsis)), const SizedBox(width: Spacing.space12), _CopyButton(...) ]`, and the surrounding closing brackets. Replace the block in place with the `_CopyButton(...)` invocation as a direct child of the `_buildBody` `Column`, preserving its three arguments verbatim: `url: url`, `copied: _copied`, and the `onCopied:` closure that runs `setState(() => _copied = true)`, `showSuccessSnackBar(context, message: 'Link copied to clipboard')`, and the 2-second `Future.delayed` reset. The `const SizedBox(height: Spacing.space20)` that follows the URL container in the current code stays as the spacer between the button and the "Include in feed:" label. Do _not_ replace the removed block with an alternative "hidden URL" widget (`Offstage`, `Visibility(visible: false)`, or a comment placeholder) — the `_CopyButton` still holds the `url` field, which is the only reader the copy operation needs.
- In `_CopyButton.build()`, change the un-copied label literal from `'Copy'` to `'Copy subscription link'`. The full expression becomes `label: copied ? 'Copied' : 'Copy subscription link'`. Do _not_ change the `'Copied'` post-tap label — Tony's Cycle 5 feedback does not touch the confirmation state, and pairing a 2-second `'Copied'` with the `'Link copied to clipboard'` snackbar is the same confirmation pattern already in place.
- In the same `_CopyButton.build()` `AppButton(...)` invocation, add `fullWidth: true`. This wraps the underlying `FButton` in `SizedBox(width: double.infinity)` per [app_button.dart:161-164](lib/components/ui/app_button.dart#L161-L164), the exact idiom used by every full-width primary-action call site in the codebase.
- Do _not_ change the `icon:` field, the `backgroundColor:` field, the `onPressed:` closure, or the `_CopyButton` constructor / fields (`url`, `copied`, `onCopied`). The public shape of `_CopyButton` is preserved; only the `AppButton` invocation grows two lines (fullWidth) and swaps a string literal (label).
- Do _not_ change the description paragraph above the removed URL container ("Add your BandRoadie events to your favorite calendar app…"), the `const SizedBox(height: Spacing.space20)` before or after it, or any downstream widget in `_buildBody` (the "Include in feed:" label, the five `AppToggleTile`s, the "How to subscribe:" block, the notes container, or the `SheetFooter`).
- Do _not_ touch `toggle_tile.dart` — Cycles 3 and 4 were already off-limits for Cycle 4 and remain off-limits for Cycle 5.
- Do _not_ touch `AppButton` — `fullWidth` already exists as a first-class parameter; no wrapper change is needed.

### 6. Cycle 6 — Match the Add Event header exactly, and move the copy button between the feed toggles and How-to-subscribe

Two coordinated widget-tree edits inside `calendar_subscription_dialog.dart`. No other file is touched.

**6a. Header — byte-exact match with Add Event's `_buildStickyHeader`.**

- Inside `_CalendarSubscriptionDialogState.build()`, replace the header block that currently spans (i) `const SizedBox(height: Spacing.space16)`, (ii) `Padding(padding: const EdgeInsets.symmetric(horizontal: Spacing.pagePadding), child: Row(children: [const Icon(AppIcons.calendar, color: AppColors.primary, size: 22), const SizedBox(width: Spacing.space12), Expanded(child: Text('Subscribe to ${widget.bandName} Calendar', style: AppTextStyles.title3)), GestureDetector(onTap: () => Navigator.of(context).pop(), child: Container(width: 32, height: 32, decoration: BoxDecoration(color: context.colors.background, shape: BoxShape.circle), child: Icon(AppIcons.close, size: 18, color: context.colors.textSecondary)))]))`, and (iii) the following `const SizedBox(height: Spacing.space16)` — with a single `Padding` widget whose inline structure matches Add Event's `_buildStickyHeader` verbatim except for the title string, which becomes the exact static literal `'Subscribe to Band Calendar'` (no `${widget.bandName}` interpolation, no band-derived substitution):

  ```dart
  Padding(
    padding: const EdgeInsets.fromLTRB(
      Spacing.pagePadding,
      20,
      Spacing.pagePadding,
      16,
    ),
    child: Row(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Expanded(
          child: Text(
            'Subscribe to Band Calendar',
            style: const TextStyle(
              fontSize: 20,
              fontWeight: FontWeight.w600,
              color: Color(0xFFFAFAFA),
            ),
          ),
        ),
        const SizedBox(width: 8),
        SizedBox(
          width: 32,
          height: 32,
          child: IconButton(
            padding: EdgeInsets.zero,
            onPressed: () => Navigator.of(context).pop(),
            icon: const Icon(
              Icons.close,
              size: 16,
              color: Color(0xFFA1A1AA),
            ),
            style: IconButton.styleFrom(
              backgroundColor: const Color(0xFF1F1F23),
              shape: RoundedRectangleBorder(
                borderRadius: BorderRadius.circular(8),
              ),
            ),
          ),
        ),
      ],
    ),
  ),
  ```

  This produces every one of Add Event's five discrete header traits — inline `TextStyle(fontSize: 20, fontWeight: FontWeight.w600, color: Color(0xFFFAFAFA))`, no leading icon, `CrossAxisAlignment.start`, `EdgeInsets.only(top: 20, bottom: 16)` vertical rhythm (with horizontal `Spacing.pagePadding` matching the existing sheet ancestor padding), and the `SizedBox(32×32, child: IconButton(padding: EdgeInsets.zero, icon: Icon(Icons.close, size: 16, color: 0xFFA1A1AA), style: IconButton.styleFrom(backgroundColor: 0xFF1F1F23, shape: RoundedRectangleBorder(radius 8))))` close control — while the title text itself is the exact static literal Tony specified (`'Subscribe to Band Calendar'`) with no active-band interpolation.

- The two surrounding `SizedBox(height: Spacing.space16)` spacers disappear; their vertical role is absorbed into the header's `Padding(EdgeInsets.only(top: 20, bottom: 16))`. The drag handle above stays verbatim — its `margin: EdgeInsets.only(top: 12)` continues to sit the handle 12px below the shell's top edge, and the header's `top: 20` gives ~20px between drag-handle bottom and title top.
- Add the file-scope import `import 'package:flutter/material.dart';` — already present (the file imports `flutter/material.dart` at line 1). No new import is required for `IconButton`, `Icons.close`, `IconButton.styleFrom`, or `RoundedRectangleBorder` — all live in `package:flutter/material.dart`. Confirm by grep before adding to avoid a duplicate import.
- Remove the leading `Icon(AppIcons.calendar, color: AppColors.primary, size: 22)` and its `SizedBox(width: Spacing.space12)` — both go with the header rewrite. After Cycle 6, the only remaining reference to `AppIcons.calendar` in this file is inside `_InstructionTile`'s `Google Calendar` row (via `AppIcons.calendarDays`) — unaffected. Grep confirms `AppIcons.close` no longer appears in the file after this cycle (the close icon becomes `Icons.close` — the Material glyph — to match Add Event's exact literal).
- Confirm by grep that `AppColors.primary` still appears at other sites in the file (the header `AppProgressIndicator(color: AppColors.primary)` in `_buildLoading` and the error `Icon(AppIcons.error, color: AppColors.error, size: 48)` — the second is `.error`, not `.primary`) before deleting the header's `AppColors.primary` reference; the loading indicator's `AppColors.primary` reference stays untouched.
- Keep the outer `Material(color: Colors.transparent)` shell, the `DecoratedBox(decoration: ..., child: Column(...))` shell, the drag handle, the `Flexible + SingleChildScrollView`, and the `SheetFooter` verbatim. The header rewrite is scoped to the block between the drag handle's `SizedBox` gap and the scroll body.
- Do _not_ move the header inside the scroll body — that would break the `Flexible` boundary that keeps `SheetFooter` sticky above the safe area, and would cause the title to scroll off-screen as the user scrolls the body. The header stays as a shell-level sibling of the drag handle and the scroll view, matching the current Subscribe Calendar shell pattern and every other Forui-backed sheet in the calendar feature (`add_block_out_drawer.dart`, `day_detail_bottom_sheet.dart`).
- Do _not_ remove or resize the drag handle — it is the Forui-sheet house pattern (per calendar-forui-consolidation architect plan) and its presence keeps this sheet visually consistent with sibling calendar sheets. Add Event uses `showModalBottomSheet` (Material), which does not use a drag handle; Subscribe Calendar uses `showAppBottomSheet` → `showFSheet` (Forui), which does. This is a legitimate cross-family difference; Cycle 6 narrows the divergence to just the drag-handle strip, not the whole header.

**6b. Body order — move the copy button between the feed toggles and How-to-subscribe.**

- Inside `_CalendarSubscriptionDialogState._buildBody(context, url)`, the current widget-tree order (after Cycle 5) reads: description `Text` → `SizedBox(height: Spacing.space20)` [gap A] → `_CopyButton(url: url, copied: _copied, onCopied: ...)` → `SizedBox(height: Spacing.space20)` [gap B] → "Include in feed:" label → `SizedBox(height: Spacing.space8)` → five `AppToggleTile`s (with `SizedBox(height: Spacing.space4)` between) → `SizedBox(height: Spacing.space20)` [gap C] → "How to subscribe:" label → …. Cycle 6 rearranges to: description `Text` → `SizedBox(height: Spacing.space20)` [gap A stays] → "Include in feed:" label → `SizedBox(height: Spacing.space8)` → five `AppToggleTile`s (unchanged) → `SizedBox(height: Spacing.space20)` [gap C stays] → `_CopyButton(url: url, copied: _copied, onCopied: ...)` → `SizedBox(height: Spacing.space20)` [gap B, moved to bracket the button after the toggles] → "How to subscribe:" label → … (rest of body unchanged).
- Concretely: cut the `_CopyButton(...)` node together with one of its two surrounding `SizedBox(height: Spacing.space20)` gaps (either gap A or gap B — pick gap B, the one _after_ `_CopyButton`); paste the pair between the toggles' trailing `SizedBox(height: Spacing.space20)` [gap C] and the "How to subscribe:" label. Net widget count in `_buildBody` is unchanged; three `Spacing.space20` gaps become three `Spacing.space20` gaps in a permuted order. The `_CopyButton` invocation itself — `url:`, `copied:`, `onCopied:` closure with `setState(() => _copied = true)`, `showSuccessSnackBar(context, message: 'Link copied to clipboard')`, and the 2-second `Future.delayed` reset — is preserved verbatim.
- Do _not_ change any argument passed to `_CopyButton`. Do _not_ change any argument passed to `AppButton` inside `_CopyButton.build()` (Cycle 5's `label: copied ? 'Copied' : 'Copy subscription link'`, `fullWidth: true`, `icon: copied ? AppIcons.check : AppIcons.copy`, `backgroundColor: copied ? context.colors.success : null`, `onPressed: () { Clipboard.setData(ClipboardData(text: url)); onCopied(); }` all stay). Do _not_ change the `_copied` state machine on `_CalendarSubscriptionDialogState`, the snackbar text, or the 2-second reset duration.
- Do _not_ change the description paragraph, the "Include in feed:" label, the "How to subscribe:" label, the notes `Container`, the three `_InstructionTile`s, the five `AppToggleTile`s, the SheetFooter, or the loading / error branches of `_buildBody`.
- Do _not_ touch [lib/shared/widgets/toggle_tile.dart](lib/shared/widgets/toggle_tile.dart) — Cycles 3, 4, 5 already treat it as off-limits and Cycle 6 continues that.
- Do _not_ touch `AppButton`, `AppSwitch`, `AppProgressIndicator`, `SheetFooter`, `showAppBottomSheet`, `app_icons.dart`, `design_tokens.dart`, `brand_colors.dart`, `app_theme.dart`, or `event_editor_theme.dart`.

**6c. Title literal + dead `bandName` parameter cleanup — module-local API expansion inside Cycle 6.**

- Inside the header `Text` widget in §6a above, the title's first argument is the exact static literal `'Subscribe to Band Calendar'` — no `${widget.bandName}` interpolation, no substring built from the active band's name, no other dynamic content of any kind. Tony's Cycle 6 amendment is byte-exact on this literal.
- In `showCalendarSubscriptionDialog(...)` at [calendar_subscription_dialog.dart:22-37](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L22-L37), remove the `required String bandName,` line from the named-argument block and the `bandName: bandName,` line from the forwarded `CalendarSubscriptionDialog(...)` invocation inside the function body. After the edit, the function signature reads `void showCalendarSubscriptionDialog(BuildContext context, WidgetRef ref, { required String bandId, })` and forwards only `bandId: bandId` into `CalendarSubscriptionDialog(...)`.
- In `CalendarSubscriptionDialog` at [calendar_subscription_dialog.dart:42-51](lib/features/calendar/widgets/calendar_subscription_dialog.dart#L42-L51), remove the `final String bandName;` field declaration and the `required this.bandName,` line from the constructor. After the edit, the class exposes exactly one field (`bandId`) and one required constructor argument (`bandId`).
- In [lib/features/calendar/calendar_screen.dart](lib/features/calendar/calendar_screen.dart#L507-L535), inside the `GestureDetector` `onTap` closure that opens the sheet, remove the `final bandName = activeBand?.name ?? 'Band';` local variable and the `bandName: bandName,` named argument in the `showCalendarSubscriptionDialog(...)` call. After the edit, the closure reads `activeBand?.id` (already assigned to `bandId`) and calls `showCalendarSubscriptionDialog(context, ref, bandId: bandId)`.
- In [lib/features/calendar/calendar_tab_content.dart](lib/features/calendar/calendar_tab_content.dart#L483-L511), inside the `GestureDetector` `onTap` closure that opens the sheet, remove the `final bandName = bandState.activeBand?.name ?? 'Band';` local variable and the `bandName: bandName,` named argument in the `showCalendarSubscriptionDialog(...)` call. After the edit, the closure reads `bandState.activeBand?.id` (already assigned to `bandId`) and calls `showCalendarSubscriptionDialog(context, ref, bandId: bandId)`.
- Do _not_ preserve `bandName` as an unused required parameter for backward compatibility. Grep of `**/*.dart` across the entire workspace confirms zero external consumers (no test file, no plugin, no marketing app, no integration harness, no reference in `bandroadie_fresh/`, no generated file). Retaining the parameter would trip `avoid_unused_constructor_parameters` on the widget field and would mislead any future reader who sees a value threaded through provider → local → arg → field for no purpose.
- Do _not_ rename `bandName` to `_bandName`, `unusedBandName`, or any other identifier to silence an analyzer warning — the correct move for a dead required parameter with zero external consumers is deletion.
- Do _not_ introduce a default value like `String bandName = 'Band'` or `String bandName = ''` at the receiving end and keep accepting the argument — deletion, not defaulting, is the plan.
- Do _not_ change any other identifier, symbol, or widget in either caller file. The two callers' surrounding `_ActionButton` / `_buildActionButtons(perms)` / `Center` / `GestureDetector` / outer `Padding` / `Text('+ Subscribe to Calendar', ...)` / `AppColors.primary` / `AppFontSizes.subhead` / `FontWeight.w500` widgets and every other line in the two `onTap` closures stay verbatim. Only the two lines directly related to `bandName` are removed at each site.

**Rejected alternatives:**

- **Swap `_CopyButton` for `SheetFooter`.** `SheetFooter` is a sticky bottom-of-sheet element with its own decoration and safe-area padding — it is not a fit for an inline row-anchored copy action. `AppButton` is the correct wrapper.
- **Replace `AppToggleTile` in this sheet with inline `AppSwitch` + `Text` rows and leave `toggle_tile.dart` alone.** This inflates the sheet body with duplicated container + padding + layout code for five rows and produces a surface-color / radius regression vs. the current tile appearance. Migrating the widget's internals is a smaller and lower-risk change than inlining five rows.
- **Change `showAppBottomSheet`'s default `mainAxisMaxRatio`.** Global default touches ~136 call sites; explicitly out of scope per every prior height-fix plan in this repo. The correct move is per-caller opt-in.
- **Swap the close-X `GestureDetector` for `FButton.icon` or an `AppIconButton`.** No `AppIconButton` wrapper exists that matches the circular-background house pattern used by `day_detail_bottom_sheet.dart`, `add_block_out_drawer.dart`, and this dialog. Changing only this sheet's close-X would produce three inconsistent close buttons in the calendar feature. _Superseded by Cycle 6:_ Tony's Cycle 6 feedback explicitly requests the close-X match Add Event's `IconButton` pattern verbatim, accepting the cross-sheet divergence within the calendar feature. The Cycle 3 concern about calendar-feature close-button parity is overridden by the Cycle 6 explicit visual ask.
- **Migrate the drag-handle / rounded-top `Container` shell to Forui.** Documented house pattern — Forui preview's `backgroundColor` / `shape` are no-ops, so this is expected and not a defect (per calendar-forui-consolidation architect plan). Not changed.
- **Cycle 4: Add a new typography token or a new `AppTextStyles` entry for these tiles.** Rejected — `AppFontSizes.body` (16) and `AppFontSizes.subhead` (14) already exist at [design_tokens.dart:353-355](lib/app/theme/design_tokens.dart#L353-L355) and are used ~200 times across the app. Adding a bespoke `AppTextStyles.subscribeInstructionTitle` (or similar) would create an unused single-purpose helper.
- **Cycle 4: Replace the four `TextStyle(...)` inline instances in `_InstructionTile` and `_NoteBullet` with `AppTextStyles.callout` / `AppTextStyles.caption` composed styles.** Rejected — the existing widgets pass `color:` per-instance from `context.colors.textPrimary` / `.textSecondary` / `.textMuted`, and `AppTextStyles.callout` bakes in a `Geist` font family without a color slot. Swapping to composed styles would either drop the color-token binding or require `.copyWith(color: ...)`, which is a larger and more error-prone change than swapping the single `fontSize` field. Minimum change: swap the `fontSize` field only.
- **Cycle 4: Also bump the calendar-URL monospace row at line 280 to `AppFontSizes.subhead`.** Rejected — that is the raw ICS URL, and Tony's feedback did not include it. Raising its size would push the URL past two lines in typical widths and lose the compact monospace treatment intended for a copy-and-paste target.
- **Cycle 5: Keep the URL visible but make the button full-width below it.** Rejected — Tony's feedback is literal: _hide_ the URL. The URL is a private token-carrying link and there is no user need to render it on screen when the copy button already handles the whole clipboard flow.
- **Cycle 5: Replace the URL container with a `SelectableText(url)` widget so users can still copy-by-selection.** Rejected — supplants Tony's stated intent (hide the URL), reintroduces the on-screen visibility of a private URL, and duplicates the copy-button affordance.
- **Cycle 5: Also change the "copied" confirmation label to `'Link copied'` or `'Subscription link copied'`.** Rejected — Tony's feedback did not touch the confirmation state. The 2-second `'Copied'` label plus the `'Link copied to clipboard'` snackbar is the same pattern used elsewhere in the app; changing it is scope creep.
- **Cycle 5: Wrap the button in a `SizedBox(width: double.infinity, child: AppButton(...))` at the call site instead of using `AppButton.fullWidth: true`.** Rejected — duplicates what the wrapper already implements one layer deeper and drifts this call site from the 23 other `fullWidth: true` sites in the codebase.
- **Cycle 5: Move `_CopyButton` into `SheetFooter`, so the copy button becomes the primary sheet action.** Rejected — `SheetFooter` is a sticky bottom-of-sheet element already occupied by "Done" here, and it is designed for terminal sheet actions (dismiss, save, delete). A copy button is an inline action inside the sheet body; the same reasoning that rejected `SheetFooter` for `_CopyButton` in Cycles 1–3 applies verbatim to Cycle 5.
- **Cycle 5: Delete `_CopyButton` and inline the `AppButton(...)` invocation into `_buildBody`.** Rejected — the `_CopyButton` widget already exists and cleanly bundles the three inputs (`url`, `copied`, `onCopied`); inlining it forces a multi-line `AppButton(...)` block with a state-management closure right inside `_buildBody`, which is a larger and more invasive edit than keeping the widget and touching two of its fields.
- **Cycle 6: Route the close-button background through a new `BrandColors` token instead of hardcoding `Color(0xFF1F1F23)`.** Rejected — the token would be single-use (four production sites already inline this exact hex: Add Event, `venue_detail_screen`, `add_financial_entry_bottom_sheet`, `view_gig_drawer`) and touching `design_tokens.dart` or `brand_colors.dart` violates Cycle 6's "no shared-file edits" scope. Adding a token here is scope creep; if this hex spreads further, a token pass belongs in its own architect cycle.
- **Cycle 6: Route the close-button icon color through `context.colors.textSecondary` and the title color through `context.colors.textPrimary` instead of hardcoding `Color(0xFFA1A1AA)` and `Color(0xFFFAFAFA)`.** Rejected — Tony asked for byte-exact match with Add Event's header, and Add Event hardcodes both hex codes. In the app's dark-only theme, tokens and literals resolve to identical pixels, but the literal form makes the "this matches Add Event" intent grep-able and prevents a future theme-token refactor from silently drifting Subscribe Calendar's header away from Add Event's. This preserves textual parity with the four sibling `IconButton.styleFrom` sites in the codebase.
- **Cycle 6: Keep `AppTextStyles.title3` as the title style and skip the inline `TextStyle`.** Rejected — `AppTextStyles.title3` sets `height: 1.25` (25px line box) while Add Event's inline style omits `height` (font-metric default line box). For a single-line title inside a `Row` with `CrossAxisAlignment.start`, the two produce a ~2.5px baseline shift; using the same inline style Add Event uses is the only way to match its vertical positioning byte-exactly. This is the same rationale as the color-literal choice above — Tony asked for exact match, and the literal form is grep-parity with Add Event.
- **Cycle 6: Migrate the close button through Forui's `FButton.icon` primitive to preserve the file's "full-height-forui" scope.** Rejected — `FButton.icon` wraps children in FButton chrome (background from `FButtonStyleDelta`, radius from theme, hover/press state) that does not produce the exact 32×32 pill with `Color(0xFF1F1F23)` background and 8px corner radius that Add Event uses. Substituting `FButton.icon` breaks byte-exact parity with Add Event, which is the explicit Cycle 6 goal. The file's Forui scope covered the `_CopyButton` and `AppToggleTile` migrations in Cycle 3 (both had ready wrappers `AppButton` / `AppSwitch`); no equivalent wrapper exists for the Add Event close-pill geometry, so the correct move is the same Material `IconButton` primitive Add Event uses. Accessibility is preserved (`IconButton` provides button-role semantics that the previous `GestureDetector` lacked — a net a11y upgrade).
- **Cycle 6: Keep the `GestureDetector` + `Container(shape: circle)` close control and only recolor it to `Color(0xFF1F1F23)` / `Color(0xFFA1A1AA)`.** Rejected — misses the 8px-radius rounded-square shape (Add Event uses `RoundedRectangleBorder`, not `BoxShape.circle`), misses the `Icons.close` Material glyph (Add Event uses Material, not `LucideIcons.x`), misses the button-role semantics `IconButton` provides, and misses the icon size 16 (currently 18). Half-matches are not byte-exact.
- **Cycle 6: Remove the drag handle above the header to fully mirror Add Event's chrome positioning.** Rejected — the drag handle is the documented Forui-sheet house pattern per [docs/features/calendar-forui-consolidation/ARCHITECT_PLAN.md](docs/features/calendar-forui-consolidation/ARCHITECT_PLAN.md#L51-L55), is present verbatim on `add_block_out_drawer.dart` and `day_detail_bottom_sheet.dart`, and Tony's Cycle 6 ask targets the header row and close control specifically. Removing it would drift this sheet away from its two Forui-sheet siblings — a separate architectural change with sibling-consistency implications that goes beyond Tony's request. Drag handle stays; the 20px `top` inside the header padding provides comfortable visual separation from the drag handle above.
- **Cycle 6: Move the header inside the scroll body (as Add Event does).** Rejected — Add Event opens via `showModalBottomSheet` (Material) with `body = Column([Expanded(SingleChildScrollView(child: _buildScrollableBody)), _buildStickyFooter])`, where the header is the first child of the scrollable body and scrolls with content. Subscribe Calendar opens via `showAppBottomSheet` (Forui `showFSheet`) with `body = Column([Material, DecoratedBox → Column([dragHandle, header, Flexible(SingleChildScrollView), SheetFooter])])`, where the header is a shell-level sibling and stays pinned above the scroll region so the title is always visible. Moving the header inside the scroll region would break the `Flexible` boundary that keeps `SheetFooter` sticky above the safe area, and would cause the title to scroll off-screen — an unintended regression from a purely visual ask. Header stays outside the scroll body.
- **Cycle 6: Add a subtitle line below the title (matching Add Event's `'Fill in what you know now. You can edit any of it later.'` pattern).** Rejected — Tony's Cycle 6 feedback did not ask for a subtitle. The description paragraph at the top of `_buildBody` ("Add your BandRoadie events to your favorite calendar app. Events will stay in sync automatically.") already fills the subtitle's narrative role for this sheet. Adding a second subtitle inside the header would duplicate that guidance and violate the "no scope creep" architect discipline.
- **Cycle 6: Keep the leading `Icon(AppIcons.calendar, ...)` to preserve visual identity.** Rejected — Add Event's header has no leading icon, and Tony's Cycle 6 ask includes "positioning" as one of four match axes. Keeping the leading icon shifts the title right of the left content edge, breaking positioning parity. The title itself ("Subscribe to `${widget.bandName}` Calendar") contains "Calendar" and the sheet's description paragraph explicitly mentions calendar apps — no user-comprehension regression from dropping the decorative icon.
- **Cycle 6: Extract the Add-Event-style close button into a shared `AppSheetCloseButton` widget so future sheets can consume it.** Rejected — Cycle 6's scope is a single sheet's visual match, not a codebase-wide extraction. The pattern currently lives at four sites (Add Event, `venue_detail_screen`, `add_financial_entry_bottom_sheet`, `view_gig_drawer`); if it spreads further, extraction belongs in its own architect cycle that surveys every consumer, defines the widget's parameter surface (`onPressed`, `tooltip`, `size`?), and handles the icon-glyph choice (Material `Icons.close` vs. `LucideIcons.x`). Inline-matching Add Event's exact tree is the minimum-change path for Cycle 6.
- **Cycle 6: Move `_CopyButton` into the `SheetFooter` alongside "Done" as a secondary action.** Rejected — Tony's Cycle 6 target explicitly places the button _in the body_ ("below the list of 'Include in feed' and above 'How to subscribe'"). Also, `SheetFooter`'s current API surfaces one primary action; adding a secondary would require an API change to a shared component. And a copy button is a body-inline action, not a terminal sheet action (same reasoning as Cycles 1–3 and Cycle 5 rejected-alternatives for this widget).
- **Cycle 6: Also change the `SizedBox(height: Spacing.space20)` bracketing the moved `_CopyButton` to a smaller value like `Spacing.space12`.** Rejected — spacing tokens are unchanged in Tony's ask. The 20px rhythm on both sides of the button matches the 20px rhythm the button had in its Cycle 5 position; preserving both gaps at `Spacing.space20` keeps the vertical rhythm consistent with the rest of the body (all body-level section breaks are `Spacing.space20`).
- **Cycle 6: Keep the header title as `'Subscribe to ${widget.bandName} Calendar'` and only restyle it.** Rejected — Tony's latest Cycle 6 amendment is explicit and verbatim: _"Also don't include the band name in the drawerr title. Title should be only 'Subscribe to Band Calendar'"_. Retaining the interpolation would keep the on-screen title band-specific and fail the amendment on the literal check.
- **Cycle 6: Change the title to a different literal like `'Subscribe'`, `'Calendar Subscription'`, `'Band Calendar Subscription'`, or `'Subscribe to Calendar'`.** Rejected — Tony specified the exact string `'Subscribe to Band Calendar'`. Any alternative literal is scope creep against a byte-exact requirement, however small the difference.
- **Cycle 6: Keep the title interpolated but always pass a hard-coded string `'Band'` from both callers (i.e. `bandName: 'Band'`).** Rejected — same on-screen result but retains all the plumbing overhead: the parameter chain, the widget field, the constructor arg, the intermediate forward, and the two `bandName: 'Band'` arguments at the call sites. The parameter would be dead-except-for-a-literal-forward and its presence would invite a future edit to reintroduce the active-band interpolation. Deletion is the honest expression of the requirement; a placeholder forward is not.
- **Cycle 6: Retain `bandName` as an unused required parameter on `showCalendarSubscriptionDialog` / `CalendarSubscriptionDialog` for backward compatibility with a hypothetical external caller.** Rejected — grep of `**/*.dart` across the workspace (including `test/**`, `bandroadie_fresh/**`, `marketing/**`, and every other Dart source root) returns exactly three call sites: the definition file and the two internal callers already read in the Existing System Analysis. No external plugin, integration test, marketing app fork, or generated file consumes this API. Retention would trip `avoid_unused_constructor_parameters` on `CalendarSubscriptionDialog.bandName` (an analyzer warning, not merely info-level) and would mislead future readers who see a value threaded through the API chain for no purpose. The correct move for a dead required parameter with zero external consumers is deletion, not preservation.
- **Cycle 6: Rename `bandName` to `_bandName` (or `unusedBandName`) inside the dialog file to silence the analyzer while retaining the parameter surface.** Rejected — worse than retention. It hides the deadness (the analyzer stays quiet but the parameter is still threaded through the API for no purpose), and a leading underscore on a public parameter is not idiomatic Dart. If a caller ever needs to pass a value again, un-renaming is the correct fix — but that is a future problem, not a present one. Deletion is the honest expression of the current state.
- **Cycle 6: Default `bandName` to `''` or `'Band'` at the receiving end (`String bandName = 'Band'`) and keep both callers passing it.** Rejected — same result as retention (dead-except-for-forwarded-literal), plus the added noise of two identical default declarations and a divergence between the caller-passed value and the default. Deletion is smaller and clearer than defaulting.
- **Cycle 6: Delete `bandName` from the dialog file but leave the two callers still passing it (as a "harmless" named argument).** Rejected — the Dart compiler would reject unknown named arguments; the callers would fail to compile. The correct move is delete-both-sides simultaneously in one Engineer commit, exactly as the Task Breakdown enumerates.

## Database Impact

n/a — no SQL, migrations, RLS policies, RPC functions, edge functions, or grants change. Layout and widget-tree change only.

## Flutter Architecture Changes

None. No new Riverpod providers, notifiers, or controllers. No new repository. No new model. Init order (`WidgetsFlutterBinding` → URL strategy → orientation → `AppVersionService.init` → `validateSupabaseConfig` → `Supabase.initialize` → `Firebase.initializeApp` [native] → `DeepLinkService` → `runApp`) unchanged. Auth / PKCE flow unchanged. Routing unchanged. Platform-conditional code unchanged (sheet renders uniformly across iOS, Android, macOS, and web).

## Files to Create

None.

## Files to Modify

| File                                                                                                                               | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart) | **Cycles 1–3 (APPROVED, already in the branch):** (a) In `showCalendarSubscriptionDialog()` (lines 22-37), add `mainAxisMaxRatio: 1.0` and `useSafeArea: true` to the `showAppBottomSheet` call. (b) In `_CalendarSubscriptionDialogState.build()` (lines 98-104), replace the shell `Container` with a `DecoratedBox`, preserving `decoration:` and `child:` verbatim; the inner `constraints:` is dropped in the process (a plain `Container` with only decoration + child trips `use_decorated_box`). Also remove the `final keyboardHeight = MediaQuery.of(context).viewInsets.bottom;` local at line 92 — after removing the inner cap, `keyboardHeight` has no remaining reader (the scroll view padding at line 178 uses `safeBottom` alone, which stays). (c) In `_CopyButton.build()` (lines 447-495), replace the `Material` + `InkWell` + `Padding` + `Row` tree with a single `AppButton(label: copied ? 'Copied' : 'Copy', icon: copied ? AppIcons.check : AppIcons.copy, backgroundColor: copied ? context.colors.success : null, onPressed: () { Clipboard.setData(...); onCopied(); })`. Do _not_ pass `variant:` — `AppButton`'s default is `AppButtonVariant.primary` ([app_button.dart:34](lib/components/ui/app_button.dart#L34)); passing it trips `avoid_redundant_argument_values`. Add the `app_button.dart` import. Drop the now-unused local `Material` / `InkWell` constructions.<br><br>**Cycle 4 (new):** (d) In `_InstructionTile.build()` at line 491, change the title `Text`'s `fontSize` from `AppFontSizes.caption` to `AppFontSizes.body`. (e) In the same `build()` at line 499, change the instruction `Text`'s `fontSize` from `AppFontSizes.caption` to `AppFontSizes.subhead`. (f) In `_NoteBullet.build()` at line 527, change the bullet-character `Text('•  ')`'s `fontSize` from `AppFontSizes.caption` to `AppFontSizes.subhead`. (g) In the same `build()` at line 534, change the body `Text(text)`'s `fontSize` from `AppFontSizes.caption` to `AppFontSizes.subhead`. Preserve every existing color binding, weight, `Icon`, layout, `SizedBox`, decoration, and constructor. Do _not_ modify the fifth `AppFontSizes.caption` reference at line 280 (the URL row). Do _not_ modify the two formatting-only diff hunks around the header `Icon` and `AppProgressIndicator` — those are the tester's approved uncommitted state and Cycle 4 must not disturb them.<br><br>**Cycle 5 (new):** (h) In `_buildBody(context, url)` (currently lines 270-311), delete the URL surface: the `// Subscription URL` comment, the wrapping `Container` (with its `padding: const EdgeInsets.all(Spacing.space12)` and its `decoration: BoxDecoration(color: context.colors.surfaceElevated, borderRadius: BorderRadius.circular(Spacing.buttonRadius), border: Border.all(color: context.colors.border))`), and the inner `Row` — including `Expanded(Text(url, style: TextStyle(color: context.colors.textSecondary, fontSize: AppFontSizes.caption, fontFamily: 'monospace'), maxLines: 2, overflow: TextOverflow.ellipsis))` and the `const SizedBox(width: Spacing.space12)`. Lift the existing `_CopyButton(url: url, copied: _copied, onCopied: ...)` invocation up to be a direct child of the `_buildBody` `Column`, preserving all three arguments verbatim; the `const SizedBox(height: Spacing.space20)` that follows stays as the spacer before "Include in feed:". (i) In `_CopyButton.build()`, change the un-copied label from `'Copy'` to `'Copy subscription link'` (full expression: `label: copied ? 'Copied' : 'Copy subscription link'`) and add `fullWidth: true` to the `AppButton(...)` invocation. Do _not_ change the `icon:`, `backgroundColor:`, `onPressed:`, or the `_CopyButton` constructor / fields (`url`, `copied`, `onCopied`). Removing the URL row incidentally takes the file's last `AppFontSizes.caption` reference with it — that is expected and does not require any token cleanup elsewhere.<br><br>**Cycle 6 (new):** (k) Inside `_CalendarSubscriptionDialogState.build()`, replace the three-piece header block (the `SizedBox(height: Spacing.space16)` above the header, the `Padding(EdgeInsets.symmetric(horizontal: Spacing.pagePadding), child: Row(children: [Icon(AppIcons.calendar, ...), SizedBox(width: Spacing.space12), Expanded(Text(..., style: AppTextStyles.title3)), GestureDetector(onTap: pop, child: Container(width: 32, height: 32, decoration: BoxDecoration(color: context.colors.background, shape: BoxShape.circle), child: Icon(AppIcons.close, size: 18, color: context.colors.textSecondary)))]))`, and the `SizedBox(height: Spacing.space16)` below the header) with a single `Padding(padding: const EdgeInsets.fromLTRB(Spacing.pagePadding, 20, Spacing.pagePadding, 16), child: Row(crossAxisAlignment: CrossAxisAlignment.start, children: [Expanded(child: Text('Subscribe to Band Calendar', style: const TextStyle(fontSize: 20, fontWeight: FontWeight.w600, color: Color(0xFFFAFAFA)))), const SizedBox(width: 8), SizedBox(width: 32, height: 32, child: IconButton(padding: EdgeInsets.zero, onPressed: () => Navigator.of(context).pop(), icon: const Icon(Icons.close, size: 16, color: Color(0xFFA1A1AA)), style: IconButton.styleFrom(backgroundColor: const Color(0xFF1F1F23), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(8)))))]))`. The leading `Icon(AppIcons.calendar, ...)` and its `SizedBox(width: Spacing.space12)` are dropped; the title's `AppTextStyles.title3` becomes an inline `TextStyle(fontSize: 20, fontWeight: FontWeight.w600, color: Color(0xFFFAFAFA))`; the close `GestureDetector`+`Container(shape: circle)` becomes `SizedBox(32×32, child: IconButton(...))` with hardcoded `Color(0xFF1F1F23)` background and 8px rounded-square shape. The two vertical `SizedBox(height: Spacing.space16)` spacers are absorbed into the header's own `EdgeInsets.fromLTRB(..., 20, ..., 16)` padding. Drag handle above the header is not touched. (l) In `_buildBody(context, url)`, cut the `_CopyButton(...)` node and one of its two surrounding `SizedBox(height: Spacing.space20)` gaps from its Cycle 5 position (between the description `Text` and the "Include in feed:" label) and paste them between the toggles' trailing `SizedBox(height: Spacing.space20)` and the "How to subscribe:" label. Net result: description → space20 → "Include in feed:" label → space8 → five toggles (with space4 between) → space20 → `_CopyButton(...)` → space20 → "How to subscribe:" label → space12 → three instruction tiles (with space8 between) → space20 → notes `Container`. The `_CopyButton` invocation and all three arguments (`url`, `copied`, `onCopied` closure) are preserved verbatim. No import change is required — `flutter/material.dart` is already imported at line 1 (source of `IconButton`, `Icons.close`, `IconButton.styleFrom`, `RoundedRectangleBorder`, `BorderRadius`). Confirm by grep that the file's remaining `AppIcons.calendar` reference count drops by one (the header removal) and the file's `AppIcons.close` reference count drops to zero (only the header used it). Note that the `Text` first argument inside the header is the exact static literal `'Subscribe to Band Calendar'` — no `${widget.bandName}` interpolation. (m) API cleanup — remove the dead `bandName` parameter from the dialog's public surface: in `showCalendarSubscriptionDialog(...)` (lines 22-37), delete `required String bandName,` from the named-arg block and delete `bandName: bandName,` from the forwarded `CalendarSubscriptionDialog(...)` invocation inside the function body; in `CalendarSubscriptionDialog` (lines 42-51), delete the `final String bandName;` field and the `required this.bandName,` line from the constructor. After the edit, the function signature reads `void showCalendarSubscriptionDialog(BuildContext context, WidgetRef ref, { required String bandId, })` and the widget exposes exactly one field (`bandId`) and one required constructor argument (`bandId`). Confirm by grep that `bandName` returns zero occurrences across the file (function param, forward, widget field, constructor arg, and the header title's former `${widget.bandName}` all gone). |
| [lib/shared/widgets/toggle_tile.dart](lib/shared/widgets/toggle_tile.dart)                                                         | **Cycle 3 (APPROVED, already in the branch):** In `AppToggleTile.build()` (line 94), replace `Switch.adaptive(value: value, onChanged: enabled ? onChanged : null, activeTrackColor: AppColors.primary, inactiveTrackColor: context.colors.surfaceOverlay, inactiveThumbColor: context.colors.textSecondary)` with `AppSwitch(value: value, onChanged: enabled ? onChanged : null)`. Add the `app_switch.dart` import. `brand_colors.dart` stays because `context.colors` still resolves through it (verified by Cycle 3 engineer report). No changes to the widget's constructor, fields, or public API.<br><br>**Cycle 4:** No change — file is off-limits for Cycle 4.<br><br>**Cycle 5:** No change — file is off-limits for Cycle 5.<br><br>**Cycle 6:** No change — file is off-limits for Cycle 6.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| [lib/features/calendar/calendar_screen.dart](lib/features/calendar/calendar_screen.dart)                                          | **Cycles 1–5:** No change — file was off-limits for the first five cycles.<br><br>**Cycle 6 (new):** Inside the `GestureDetector` `onTap` closure at lines 507-535 that opens the Subscribe Calendar sheet, remove the local `final bandName = activeBand?.name ?? 'Band';` variable declaration and the `bandName: bandName,` named argument from the `showCalendarSubscriptionDialog(...)` call. After the edit, the closure reads `activeBand?.id` (already assigned to `bandId`) and calls `showCalendarSubscriptionDialog(context, ref, bandId: bandId)` with `bandId:` as the only named argument. Every other line in the closure and every surrounding widget (`Center`, outer `GestureDetector`, `Padding`, `Text('+ Subscribe to Calendar', ...)`, `AppColors.primary`, `AppFontSizes.subhead`, `FontWeight.w500`) stays verbatim. This is a mechanical dead-argument removal driven by the dialog-side `bandName` parameter deletion in Cycle 6; grep of `bandName` in this file's `onTap` closure returns zero occurrences after the edit. |
| [lib/features/calendar/calendar_tab_content.dart](lib/features/calendar/calendar_tab_content.dart)                                | **Cycles 1–5:** No change — file was off-limits for the first five cycles.<br><br>**Cycle 6 (new):** Inside the `GestureDetector` `onTap` closure at lines 483-511 that opens the Subscribe Calendar sheet, remove the local `final bandName = bandState.activeBand?.name ?? 'Band';` variable declaration and the `bandName: bandName,` named argument from the `showCalendarSubscriptionDialog(...)` call. After the edit, the closure reads `bandState.activeBand?.id` (already assigned to `bandId`) and calls `showCalendarSubscriptionDialog(context, ref, bandId: bandId)` with `bandId:` as the only named argument. Every other line in the closure and every surrounding widget (`Center`, outer `GestureDetector`, `Padding`, `Text('+ Subscribe to Calendar', ...)`, `AppColors.primary`, `AppFontSizes.subhead`, `FontWeight.w500`) stays verbatim. This is a mechanical dead-argument removal driven by the dialog-side `bandName` parameter deletion in Cycle 6; grep of `bandName` in this file's `onTap` closure returns zero occurrences after the edit. |

## Files Off-Limits

| File                                                                                                                                                                                                                                                                                                                                                               | Why off-limits                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| [lib/components/ui/app_bottom_sheet.dart](lib/components/ui/app_bottom_sheet.dart)                                                                                                                                                                                                                                                                                 | Shared wrapper. Its `mainAxisMaxRatio ?? (9 / 16)` fallback and `useSafeArea` forwarding are already correct — this fix opts in at the call site. Changing the default cascades to ~136 sheets.                                            |
| [lib/components/ui/app_button.dart](lib/components/ui/app_button.dart)                                                                                                                                                                                                                                                                                             | Forui `FButton` wrapper. Consumed as-is; no API change required.                                                                                                                                                                           |
| [lib/components/ui/app_switch.dart](lib/components/ui/app_switch.dart)                                                                                                                                                                                                                                                                                             | Forui `FSwitch` wrapper. Consumed as-is; no API change required.                                                                                                                                                                           |
| [lib/components/ui/sheet_footer.dart](lib/components/ui/sheet_footer.dart)                                                                                                                                                                                                                                                                                         | Already Forui-backed via `AppButton`. Unchanged.                                                                                                                                                                                           |
| [lib/components/ui/app_progress_indicator.dart](lib/components/ui/app_progress_indicator.dart)                                                                                                                                                                                                                                                                     | Already Forui-backed via `FCircularProgress`. Unchanged.                                                                                                                                                                                   |
| [lib/app/theme/app_theme.dart](lib/app/theme/app_theme.dart)                                                                                                                                                                                                                                                                                                       | App-wide `FSwitchStyleDelta`. Provides the switch styling the migrated `AppToggleTile` will inherit. Not modified.                                                                                                                         |
| [lib/features/calendar/calendar_screen.dart](lib/features/calendar/calendar_screen.dart), [lib/features/calendar/calendar_tab_content.dart](lib/features/calendar/calendar_tab_content.dart)                                                                                                                                                                       | **Cycles 1–5:** Off-limits — call sites of `showCalendarSubscriptionDialog` that already pass the required `bandId` / `bandName`; sheet chrome was owned by the dialog, not the callers. **Cycle 6:** Now on-limits (see Files to Modify above). Cycle 6's dialog-side deletion of `bandName` requires the mirrored deletion of the two callers' `final bandName = ...` locals and their `bandName: bandName,` named arguments. Every other line in each caller — the entire `Center`, outer `GestureDetector`, `Padding`, `Text('+ Subscribe to Calendar', ...)`, and every surrounding widget — remains off-limits.  |
| [lib/features/calendar/widgets/add_block_out_drawer.dart](lib/features/calendar/widgets/add_block_out_drawer.dart), [lib/features/calendar/widgets/day_detail_bottom_sheet.dart](lib/features/calendar/widgets/day_detail_bottom_sheet.dart), [lib/features/calendar/widgets/view_block_out_drawer.dart](lib/features/calendar/widgets/view_block_out_drawer.dart) | Sibling calendar sheets. Reference patterns for the fix; not the subject of this bug. Consistent close-X / drag-handle / shell pattern must remain visually identical to this sheet after the fix (the fix does not touch those elements). |
| [lib/features/calendar/calendar_subscription_service.dart](lib/features/calendar/calendar_subscription_service.dart), the `calendarBandSubscriptionUrlProvider`, `CalendarFeedPreferences` model, `getBandSubscriptionPreferences`, `updateBandSubscriptionPreferences`                                                                                            | Data layer for subscription URL and feed preferences. Bug is layout + widget-tree only.                                                                                                                                                    |
| `supabase/migrations/**`, `supabase/functions/**`                                                                                                                                                                                                                                                                                                                  | No DB or RPC change.                                                                                                                                                                                                                       |
| `pubspec.yaml`, `pubspec.lock`, `analysis_options.yaml`                                                                                                                                                                                                                                                                                                            | No dependency, package, or lint change. `forui`, `flutter_riverpod`, and every other dependency stay at their current versions.                                                                                                            |
| [lib/app/theme/design_tokens.dart](lib/app/theme/design_tokens.dart)                                                                                                                                                                                                                                                                                               | Cycle 4 does not add a token. `AppFontSizes.body` (16) and `AppFontSizes.subhead` (14) already exist and are consumed ~200 times app-wide. Off-limits.                                                                                     |
| [lib/shared/utils/snackbar_helper.dart](lib/shared/utils/snackbar_helper.dart)                                                                                                                                                                                                                                                                                     | Cycle 5 uses `showSuccessSnackBar(context, message: 'Link copied to clipboard')` unchanged. Off-limits.                                                                                                                                    |
| [lib/features/events/widgets/event_editor_drawer.dart](lib/features/events/widgets/event_editor_drawer.dart)                                                                                                                                                                                                                                                       | Cycle 6 reference source only. `_buildStickyHeader` is the visual target Subscribe Calendar's header will mirror; Add Event's own widget tree is not modified. Off-limits.                                                                 |
| [lib/features/events/widgets/event_editor_theme.dart](lib/features/events/widgets/event_editor_theme.dart)                                                                                                                                                                                                                                                         | Cycle 6 source of the `kEdCardBorder = Color(0xFF1F1F23)` constant referenced in the close-button rationale. Subscribe Calendar hardcodes the hex literal inline (matching Add Event's own `IconButton.styleFrom` inline literal); this file is neither imported nor modified. Off-limits. |
| [lib/app/theme/app_icons.dart](lib/app/theme/app_icons.dart), [lib/app/theme/design_tokens.dart](lib/app/theme/design_tokens.dart), [lib/app/theme/brand_colors.dart](lib/app/theme/brand_colors.dart), [lib/app/theme/app_theme.dart](lib/app/theme/app_theme.dart)                                                                                                | Cycle 6 does not add a token or icon and does not modify existing ones. `AppTextStyles.title3` is replaced by an inline `TextStyle` at the single call site inside `calendar_subscription_dialog.dart`; the token itself is unchanged. Off-limits. |
| Every non-listed file in `lib/`                                                                                                                                                                                                                                                                                                                                    | Layout-plus-Forui-wrapper-swap only. Cycle 4 typography edits, Cycle 5 URL-hide + copy-button edits, and Cycle 6 header + body-order edits are all confined to `calendar_subscription_dialog.dart`.                                        |

## Change Budget

| Metric                                                                                                                            | Expected                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Files created                                                                                                                     | 0                                                                                                                                                                                                                                                                                                                      |
| Files modified (cumulative across Cycles 1–6)                                                                                     | 4 (`calendar_subscription_dialog.dart`, `toggle_tile.dart`, `calendar_screen.dart`, `calendar_tab_content.dart`)                                                                                                                                                                                                       |
| Files modified in Cycle 6 only                                                                                                    | 3 (`calendar_subscription_dialog.dart`, `calendar_screen.dart`, `calendar_tab_content.dart`)                                                                                                                                                                                                                          |
| Net line delta in [calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart) — Cycles 1–3 | approximately **−31** (drop ~40-line `_CopyButton` tree, drop 3-line inner `BoxConstraints`, drop 1-line `keyboardHeight` local, swap shell `Container` → `DecoratedBox` (net zero), replace with ~10-line `AppButton` invocation without redundant `variant:`, add `mainAxisMaxRatio` + `useSafeArea` + import lines) |
| Net line delta in [calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart) — Cycle 4 only | approximately **0** (four in-place `AppFontSizes.caption` → `AppFontSizes.body` or `AppFontSizes.subhead` token swaps; ±1 line if any of the four `TextStyle(...)` blocks re-wraps under `dart format`)                                                                                                                 |
| Net line delta in [calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart) — Cycle 5 only | approximately **−16** (drop `// Subscription URL` comment, the wrapping `Container` + `padding` + `decoration`, the `Row` + `Expanded(Text(url, …))` + `SizedBox` between the text and button — roughly 18 lines removed; add `fullWidth: true` inside the existing `AppButton(...)` invocation — 1 line added; swap the un-copied label literal `'Copy'` → `'Copy subscription link'` — 0 net) |
| Net line delta in [calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart) — Cycle 6 only | approximately **−1** ± 5 (header rewrite: drop leading `Icon` + `SizedBox(width: space12)` = −2; drop two vertical `SizedBox(height: space16)` around the header = −2; swap `AppTextStyles.title3` → inline 4-line `TextStyle` inside `Text` = +3; add `crossAxisAlignment: CrossAxisAlignment.start,` to `Row` = +1; add `SizedBox(width: 8)` between title and close = +1; swap close `GestureDetector` + `Container` + nested `Icon` (~13 lines) for `SizedBox(32×32) + IconButton(padding, onPressed, icon: Icon(Icons.close, size 16, color 0xFFA1A1AA), style: IconButton.styleFrom(backgroundColor 0xFF1F1F23, shape: RoundedRectangleBorder(radius 8)))` (~16 lines) = +3; merge horizontal `Padding(EdgeInsets.symmetric(horizontal: pagePadding))` into `Padding(EdgeInsets.fromLTRB(pagePadding, 20, pagePadding, 16))` on the row = 0 net; body-order shuffle of `_CopyButton` + one `SizedBox(height: space20)` from one position to another = 0 net; title literal `'Subscribe to ${widget.bandName} Calendar'` → `'Subscribe to Band Calendar'` = 0 net; API cleanup — drop `required String bandName,` from function param + `bandName: bandName,` from forwarded call + `final String bandName;` widget field + `required this.bandName,` from constructor = −4) |
| Net line delta in [calendar_screen.dart](lib/features/calendar/calendar_screen.dart) — Cycle 6 only                              | approximately **−2** (drop local `final bandName = activeBand?.name ?? 'Band';` = −1; drop `bandName: bandName,` named argument in `showCalendarSubscriptionDialog(...)` call = −1)                                                                                                                                    |
| Net line delta in [calendar_tab_content.dart](lib/features/calendar/calendar_tab_content.dart) — Cycle 6 only                    | approximately **−2** (drop local `final bandName = bandState.activeBand?.name ?? 'Band';` = −1; drop `bandName: bandName,` named argument in `showCalendarSubscriptionDialog(...)` call = −1)                                                                                                                          |
| Net line delta in [calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart) — cumulative Cycles 1–6 | approximately **−58** ± 5 (measured against `main`; pre-Cycle-6 state on the branch is `−57` per `git diff --stat main` at the time of Cycle 6 planning: 35 insertions, 92 deletions; Cycle 6 net ≈ −1 lands the cumulative at ≈ −58) |
| Net line delta in [toggle_tile.dart](lib/shared/widgets/toggle_tile.dart)                                                         | approximately **−3** in Cycle 3; **0** in Cycle 4; **0** in Cycle 5; **0** in Cycle 6 (file untouched)                                                                                                                                                                                                                 |
| New public classes / methods                                                                                                      | 0                                                                                                                                                                                                                                                                                                                      |
| New dependencies                                                                                                                  | 0                                                                                                                                                                                                                                                                                                                      |
| New migrations / edge functions                                                                                                   | 0                                                                                                                                                                                                                                                                                                                      |
| Files touched outside `lib/`                                                                                                      | 0 (aside from this plan document)                                                                                                                                                                                                                                                                                      |

## System Impact Map

| System                                  | Impact                                                                                                                                                                                                                            |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Gigs                                    | Unaffected — no gig data path touched; the Subscribe to Calendar sheet only reads user preferences and displays the ICS URL.                                                                                                      |
| Rehearsals                              | Unaffected — same as gigs.                                                                                                                                                                                                        |
| Setlists                                | Unaffected.                                                                                                                                                                                                                       |
| Members                                 | Unaffected.                                                                                                                                                                                                                       |
| Auth                                    | Unaffected — sheet does not touch auth/session/PKCE.                                                                                                                                                                              |
| Routing                                 | Unaffected — no navigation change. Same two call sites, same widget instantiation.                                                                                                                                                |
| Notifications                           | Unaffected.                                                                                                                                                                                                                       |
| Platforms (iOS / Android / macOS / Web) | Layout change applies uniformly; sheet chrome renders through Forui's `showFSheet` on every platform. Keyboard inset handling unchanged. Web deep-link (`/auth/confirm`) unaffected — subscription sheet is not in the auth flow. |
| Init order                              | Unaffected — no change to `main.dart` or startup sequence.                                                                                                                                                                        |
| ICS feed generation (edge function)     | Unaffected — the sheet reads the URL from `calendarBandSubscriptionUrlProvider` and toggles feed preferences via `updateBandSubscriptionPreferences`; neither path changes.                                                       |
| RLS / RPC                               | Unaffected — no policy or RPC change.                                                                                                                                                                                             |

## Regression Risk

**LOW.**

Scope is two files, one widget tree, two Forui-wrapper drop-in swaps, one two-line addition at a single `showAppBottomSheet` call site, and (Cycle 4) four in-place `AppFontSizes.caption` → `AppFontSizes.body` / `AppFontSizes.subhead` field swaps inside two private, file-local widgets. The change does not touch:

- Auth, session, PKCE, or deep-link flow.
- Supabase queries, RLS, RPC functions, edge functions, or any DB path.
- Any Riverpod provider or notifier (the sheet's use of `calendarBandSubscriptionUrlProvider` and `calendarSubscriptionServiceProvider` remains identical).
- Routing, navigation, or the `activeBandProvider` band-isolation contract.
- Init order or platform-conditional code.
- The keyboard-inset handling inside the sheet (`viewInsets.bottom` / `padding.bottom` reads at lines 92-93 are preserved and continue to feed the scroll view's bottom padding).
- Sibling calendar sheets (`add_block_out_drawer.dart`, `day_detail_bottom_sheet.dart`, `view_block_out_drawer.dart`) — their drag-handle + shell patterns must stay identical to this sheet after Cycles 1–5, which the plan preserves. Cycle 6 explicitly diverges only the close-X control on this sheet to match Add Event; siblings are unaffected and remain on the calendar-feature `GestureDetector` + `Container(shape: circle)` close pattern.
- `AppFontSizes.body` / `AppFontSizes.subhead` token _values_ (Cycle 4 consumes them; it does not touch `design_tokens.dart`).
- The URL data source (`calendarBandSubscriptionUrlProvider`), the copy target (`Clipboard.setData(ClipboardData(text: url))`), the copy confirmation snackbar (`'Link copied to clipboard'`), and the copy button's icon / success-color transition / 2-second reset are all preserved by Cycle 5 — only the on-screen URL surface and the `AppButton.label` / `AppButton.fullWidth` fields change.

Four soft risks worth naming:

1. **AppToggleTile switch appearance shifts from hard rose to soft rose in the selected state.** The theme delta uses `AppColors.primarySoft` for `FSwitchVariant.selected` and `AppColors.switchTrackOff` for off. This is the app-wide unified appearance established by `unified-forui-switch-styling` and is the _intended_ target state for every switch in the app. Any earlier hard-rose visual on these five toggles was a legacy artifact of `AppToggleTile` not yet having been migrated. Non-regression against the app-wide standard; visible-but-desired against this sheet's prior rendering.
2. **`_CopyButton`'s copied-state background comes from `context.colors.success`, not a Forui variant.** Forui's `FButtonVariant` does not include a "success" variant. Using `AppButton.backgroundColor: context.colors.success` routes through the wrapper's `FButtonStyleDelta.decoration` override — the same code path already used by other `AppButton` call sites that need non-standard fills. The copied → uncopied transition is preserved.
3. **Cycle 4: The "How to subscribe" block's visual hierarchy shifts.** By design. The calendar-name labels grow from 13→16px and the platform-subtext + notes-bullets grow from 13→14px. Vertical rhythm inside the sheet increases modestly, but the surrounding `SizedBox` spacing and the notes `Container` padding are unchanged; the sheet's outer chrome (drag handle, header, `SheetFooter`) is untouched. Visible-and-desired change; no regression against any other consumer of these tokens because Cycle 4 does not modify `AppFontSizes.body` or `AppFontSizes.subhead` themselves.
4. **Cycle 5: The URL surface disappears and the copy button spans the sheet body's full content width.** By design. The URL is a private token-carrying link and there is no user-facing reason to render it on screen. The full-width primary button (`AppButton.fullWidth: true`) matches the primary-action pattern used inside `SheetFooter` and every other sheet-body primary action in the codebase; the visual weight in the sheet body increases modestly, but the sheet's outer chrome (drag handle, header, `SheetFooter` Done button, scroll behavior) is untouched. Visible-and-desired change against this sheet's prior rendering; non-regression against the codebase's `fullWidth: true` convention. Accessibility: the button's label reads `Copy subscription link` — more descriptive than the prior `Copy`; the tap target grows (full-width vs. intrinsic-width), which only improves reachability; and no `Semantics(...)` wrapper is required because `FButton` already emits button-role semantics.
5. **Cycle 6: The header restyles to match Add Event exactly and the copy button reslots between the feed toggles and How-to-subscribe.** By design. Five discrete header traits change verbatim to match Add Event: title style becomes an inline `TextStyle(fontSize: 20, fontWeight: FontWeight.w600, color: Color(0xFFFAFAFA))` (byte-equivalent to `AppTextStyles.title3` in dark mode except for a ~2.5px line-box height shift), leading `Icon(AppIcons.calendar)` disappears (title becomes left-anchored), `Row.crossAxisAlignment` becomes `CrossAxisAlignment.start`, header vertical padding shifts from `16 / 16` to `20 / 16`, and close control switches from `GestureDetector` + `Container(shape: circle, color: 0xFF09090B)` + `Icon(AppIcons.close /* LucideIcons.x */, 18, textSecondary)` to `SizedBox(32×32) + IconButton(padding: zero, icon: Icon(Icons.close /* Material */, 16, 0xFFA1A1AA), style: IconButton.styleFrom(backgroundColor: 0xFF1F1F23, shape: RoundedRectangleBorder(radius 8)))`. The close-button change is a net accessibility upgrade (`IconButton` provides button-role semantics, focus/keyboard activation, and material splash that `GestureDetector` lacks). The body-order shuffle moves `_CopyButton` from between the description and "Include in feed:" to between the toggles and "How to subscribe:"; every argument passed to `_CopyButton` and its inner `AppButton(...)` invocation (label, fullWidth, icon, backgroundColor, onPressed, `_copied` state, snackbar text, 2-second reset) is preserved verbatim. Visible-and-desired change per Tony's Cycle 6 feedback; non-regression against every downstream widget, provider, service, or DB path. The sheet's outer shell (`Material` transparent wrapper, `DecoratedBox` shell, drag handle above header, `Flexible + SingleChildScrollView`, `SheetFooter` Done button, keyboard-inset handling) is untouched. Sibling calendar sheets (`add_block_out_drawer.dart`, `day_detail_bottom_sheet.dart`, `view_block_out_drawer.dart`) still share the drag handle + rounded-top shell pattern with this sheet — Cycle 6 narrows the header/close-button divergence with those siblings to a per-sheet visual choice that Tony explicitly approved. Add Event's own header is untouched. No behavioral or data change: `Navigator.of(context).pop()` remains the close action, `showCalendarSubscriptionDialog` remains the entry point, `calendarBandSubscriptionUrlProvider` still supplies the URL, and every `AppToggleTile` / `_InstructionTile` / `_NoteBullet` still renders with its Cycle 3–5 behavior.

## Engineer Task Breakdown

### Cycles 1–3 (APPROVED and already implemented on `bug/subscribe-calendar-full-height-forui`)

1. In [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart), inside `showCalendarSubscriptionDialog()` (lines 22-37 as of `bug/subscribe-calendar-full-height-forui@HEAD`), add `mainAxisMaxRatio: 1.0,` and `useSafeArea: true,` to the `showAppBottomSheet(...)` argument list. Preserve `context`, `isScrollControlled`, `backgroundColor`, and `builder`. Make no other change in this function.
2. In the same file, inside `_CalendarSubscriptionDialogState.build()` (lines 96-105 as of `HEAD`), replace the shell `Container` with a `DecoratedBox`, preserving its `decoration:` and `child:` verbatim; the inner `constraints:` is dropped in the process. Then remove the now-unused `final keyboardHeight = MediaQuery.of(context).viewInsets.bottom;` local at line 92 — after removing the inner cap, only `safeBottom` is still consumed (by the scroll view padding at line 178). Do _not_ remove `safeBottom`. Do _not_ refactor any other part of `build()` — outer `Material(color: Colors.transparent)`, the `Column`, drag handle, header, `Flexible` + `SingleChildScrollView`, and `SheetFooter` stay verbatim.
3. In the same file, replace the entire body of `_CopyButton.build()` (lines 452-495) with a single `AppButton(...)` invocation. Fields: `label: copied ? 'Copied' : 'Copy'`, `icon: copied ? AppIcons.check : AppIcons.copy`, `backgroundColor: copied ? context.colors.success : null`, `onPressed: () { Clipboard.setData(ClipboardData(text: url)); onCopied(); }`. Do _not_ pass `variant:` — `AppButton`'s default is `AppButtonVariant.primary` ([app_button.dart:34](lib/components/ui/app_button.dart#L34)); passing it explicitly trips `avoid_redundant_argument_values`. Add `import '../../../components/ui/app_button.dart';` to the file's import block. Leave the `_CopyButton` constructor and fields (`url`, `copied`, `onCopied`) unchanged. Verify no residual `Material` / `InkWell` / `Padding` / `Row` remain inside `_CopyButton`.
4. In [lib/shared/widgets/toggle_tile.dart](lib/shared/widgets/toggle_tile.dart), inside `AppToggleTile.build()` (lines 94-100 as of `HEAD`), replace the entire `Switch.adaptive(...)` block with `AppSwitch(value: value, onChanged: enabled ? onChanged : null)`. Add `import '../../components/ui/app_switch.dart';` to the file's imports. Run a `grep AppColors` inside `toggle_tile.dart` after the swap — if zero references remain, remove the `brand_colors.dart` import; otherwise leave it. Do not change `AppToggleTile`'s constructor, fields, `Container` outer decoration, `compact` padding logic, or subtitle layout.

### Cycle 4 (implement on top of the uncommitted Cycle 1–3 state)

5. In [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart), inside `_InstructionTile.build()`, locate the title `Text` whose `TextStyle` contains `color: context.colors.textPrimary` and `fontWeight: FontWeight.w500` (line 491 as of the branch's uncommitted state). Change its `fontSize: AppFontSizes.caption` to `fontSize: AppFontSizes.body`. Do not modify `color`, `fontWeight`, the enclosing `Text`, the parent `Column`, or the `Row` layout.
6. In the same `_InstructionTile.build()`, locate the instruction `Text` whose `TextStyle` contains `color: context.colors.textSecondary` (line 499 as of the branch's uncommitted state). Change its `fontSize: AppFontSizes.caption` to `fontSize: AppFontSizes.subhead`. Do not modify `color`, the enclosing `Text`, or any surrounding widget.
7. In [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart), inside `_NoteBullet.build()`, locate the bullet-character `Text('•  ')` at line 527 as of the branch's uncommitted state. Change its `fontSize: AppFontSizes.caption` to `fontSize: AppFontSizes.subhead`. Preserve `color: context.colors.textMuted`.
8. In the same `_NoteBullet.build()`, locate the body `Text(text)` at line 534 as of the branch's uncommitted state. Change its `fontSize: AppFontSizes.caption` to `fontSize: AppFontSizes.subhead`. Preserve `color: context.colors.textMuted`.
9. Confirm by grep that the fifth `AppFontSizes.caption` reference in the file (the URL row at line 280) remains _unchanged_ under Cycle 4 alone — Cycle 4 explicitly leaves it alone. (Cycle 5 below removes this reference as a side effect of removing the URL row; Engineer must not attempt to preserve it during Cycle 5 execution.)
10. Confirm by grep that no numeric-literal `fontSize:` was introduced anywhere in the file — only token references (`AppFontSizes.body`, `AppFontSizes.subhead`, `AppFontSizes.caption`) are permitted.
11. Confirm by `git diff` that the two Cycle 1–3 formatting-only hunks (the header `Icon` line-wrap and the `AppProgressIndicator()` whitespace collapse) remain _in_ the working tree — Cycle 4 must not undo them, and must not add new formatting-only hunks.
12. Do not touch [lib/shared/widgets/toggle_tile.dart](lib/shared/widgets/toggle_tile.dart) — Cycle 3 changes remain in place and are off-limits for Cycle 4.

### Cycle 5 (new — implement on top of the uncommitted Cycle 1–4 state)

13. In [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart), inside `_CalendarSubscriptionDialogState._buildBody(context, url)`, delete the entire URL surface block that currently spans roughly lines 270-311 of the branch state. That block starts at the `// Subscription URL` comment and ends at the closing brace of the wrapping `Container`; it includes the `Container(padding: const EdgeInsets.all(Spacing.space12), decoration: BoxDecoration(color: context.colors.surfaceElevated, borderRadius: BorderRadius.circular(Spacing.buttonRadius), border: Border.all(color: context.colors.border)), child: Row(children: [Expanded(Text(url, style: TextStyle(color: context.colors.textSecondary, fontSize: AppFontSizes.caption, fontFamily: 'monospace'), maxLines: 2, overflow: TextOverflow.ellipsis)), const SizedBox(width: Spacing.space12), _CopyButton(url: url, copied: _copied, onCopied: () { ... })]))`. Lift the existing `_CopyButton(url: url, copied: _copied, onCopied: () { setState(() => _copied = true); showSuccessSnackBar(context, message: 'Link copied to clipboard'); Future.delayed(const Duration(seconds: 2), () { if (mounted) setState(() => _copied = false); }); })` invocation up so it becomes a direct child of the surrounding `_buildBody` `Column`, preserving all three arguments verbatim. The `const SizedBox(height: Spacing.space20)` that currently follows the URL container stays put as the spacer between the button and the "Include in feed:" label.
14. Confirm by grep that `Expanded(` no longer appears inside `_buildBody` (a grep of `Expanded(` scoped to the file returns zero occurrences after this edit — no other `Expanded` lives in `_buildBody` currently).
15. In [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart), inside `_CopyButton.build()`, change the `label:` field from `copied ? 'Copied' : 'Copy'` to `copied ? 'Copied' : 'Copy subscription link'`. Change only the un-copied string; leave the copied string as `'Copied'`. Do not touch `icon:`, `backgroundColor:`, or `onPressed:`.
16. In the same `_CopyButton.build()` `AppButton(...)` invocation, add `fullWidth: true,` as a new named argument (idiomatic placement: immediately after `label:` or wherever `dart format` prefers). Do not add `variant:` — the default `AppButtonVariant.primary` still applies.
17. Confirm by grep that `AppFontSizes.caption` no longer appears anywhere in `calendar_subscription_dialog.dart` — after Cycle 5 deletes the URL row, the file's last `caption` reference is gone. This supersedes the Cycle 4 check that required exactly one caption reference to remain; the Cycle 4 verification section below is amended accordingly.
18. Confirm by grep that no `SelectableText(`, `Offstage(`, `Visibility(visible: false`, `Opacity(opacity: 0`, or `SizedBox.shrink(child: Text(url` construct was substituted for the removed URL text — the URL must not appear in the widget tree at all (hidden or otherwise). It is passed only as an argument to `_CopyButton(url: url, ...)` and lives only inside `Clipboard.setData(ClipboardData(text: url))` in the `AppButton.onPressed` closure.
19. Do not touch [lib/shared/widgets/toggle_tile.dart](lib/shared/widgets/toggle_tile.dart) — Cycle 3 changes remain in place and are off-limits for Cycle 5.
20. Confirm by `git diff` that the two Cycle 1–3 formatting-only hunks (header `Icon` line-wrap and `AppProgressIndicator()` whitespace collapse) and the four Cycle 4 typography edits remain in the working tree — Cycle 5 must not undo them and must not add new formatting-only hunks elsewhere in the file.

### Cycle 6 (new — implement on top of the uncommitted Cycle 1–5 state)

21. In [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart), inside `_CalendarSubscriptionDialogState.build()`, delete the three-piece current header block: (i) the `const SizedBox(height: Spacing.space16)` above the header row (currently the sibling immediately after the drag-handle `Container`), (ii) the entire `Padding(padding: const EdgeInsets.symmetric(horizontal: Spacing.pagePadding), child: Row(children: [const Icon(AppIcons.calendar, color: AppColors.primary, size: 22), const SizedBox(width: Spacing.space12), Expanded(child: Text('Subscribe to ${widget.bandName} Calendar', style: AppTextStyles.title3)), GestureDetector(onTap: () => Navigator.of(context).pop(), child: Container(width: 32, height: 32, decoration: BoxDecoration(color: context.colors.background, shape: BoxShape.circle), child: Icon(AppIcons.close, size: 18, color: context.colors.textSecondary)))]))` block, and (iii) the `const SizedBox(height: Spacing.space16)` below the header row. Replace all three with a single `Padding` widget whose inline structure matches Add Event's `_buildStickyHeader` verbatim, except the title's first argument is the exact static literal `'Subscribe to Band Calendar'` (no `${widget.bandName}` interpolation, no band-derived substitution — Tony's Cycle 6 amendment is byte-exact on this literal):
    ```dart
    Padding(
      padding: const EdgeInsets.fromLTRB(
        Spacing.pagePadding,
        20,
        Spacing.pagePadding,
        16,
      ),
      child: Row(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Expanded(
            child: Text(
              'Subscribe to Band Calendar',
              style: const TextStyle(
                fontSize: 20,
                fontWeight: FontWeight.w600,
                color: Color(0xFFFAFAFA),
              ),
            ),
          ),
          const SizedBox(width: 8),
          SizedBox(
            width: 32,
            height: 32,
            child: IconButton(
              padding: EdgeInsets.zero,
              onPressed: () => Navigator.of(context).pop(),
              icon: const Icon(
                Icons.close,
                size: 16,
                color: Color(0xFFA1A1AA),
              ),
              style: IconButton.styleFrom(
                backgroundColor: const Color(0xFF1F1F23),
                shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.circular(8),
                ),
              ),
            ),
          ),
        ],
      ),
    ),
    ```
    Every literal (the title string `'Subscribe to Band Calendar'`, `20`, `16`, `8`, `32`, `Color(0xFFFAFAFA)`, `Color(0xFFA1A1AA)`, `Color(0xFF1F1F23)`, `Icons.close` size `16`, radius `8`) must appear verbatim. The title string must not be interpolated, template-literalized, concatenated with any band-derived expression, or otherwise made dynamic.
21b. In the same file, remove the dead `bandName` parameter from the dialog's public API. Two edits inside `showCalendarSubscriptionDialog(...)` at lines 22-37: delete the `required String bandName,` line from the named-argument block, and delete the `bandName: bandName,` line from the forwarded `CalendarSubscriptionDialog(...)` invocation inside the function body. Two edits inside the `CalendarSubscriptionDialog` class at lines 42-51: delete the `final String bandName;` field declaration, and delete the `required this.bandName,` line from the constructor. After the edits, the function signature reads `void showCalendarSubscriptionDialog(BuildContext context, WidgetRef ref, { required String bandId, })` and forwards only `bandId: bandId` into `CalendarSubscriptionDialog(...)`; the widget exposes exactly one field (`bandId`) and one required constructor argument (`bandId`). Confirm by grep that `bandName` and `widget.bandName` return zero occurrences across the file (function param, forward, widget field, constructor arg, and the header title's former `widget.bandName` reader all gone).
21c. In [lib/features/calendar/calendar_screen.dart](lib/features/calendar/calendar_screen.dart) at lines 507-535, inside the `GestureDetector` `onTap` closure that opens the Subscribe Calendar sheet, remove the local `final bandName = activeBand?.name ?? 'Band';` variable declaration and the `bandName: bandName,` named argument in the `showCalendarSubscriptionDialog(...)` call. After the edit, the closure body reads only `final activeBand = ref.read(activeBandProvider).activeBand;` + `final bandId = activeBand?.id;` + `if (bandId != null) { showCalendarSubscriptionDialog(context, ref, bandId: bandId); }`. Do _not_ change any other identifier or widget in the file. Confirm by grep that `bandName` returns zero occurrences inside this `onTap` closure after the edit.
21d. In [lib/features/calendar/calendar_tab_content.dart](lib/features/calendar/calendar_tab_content.dart) at lines 483-511, inside the `GestureDetector` `onTap` closure that opens the Subscribe Calendar sheet, remove the local `final bandName = bandState.activeBand?.name ?? 'Band';` variable declaration and the `bandName: bandName,` named argument in the `showCalendarSubscriptionDialog(...)` call. After the edit, the closure body reads only `final bandState = ref.read(activeBandProvider);` + `final bandId = bandState.activeBand?.id;` + `if (bandId != null) { showCalendarSubscriptionDialog(context, ref, bandId: bandId); }`. Do _not_ change any other identifier or widget in the file. Confirm by grep that `bandName` returns zero occurrences inside this `onTap` closure after the edit.
22. Confirm by grep — scoped to `calendar_subscription_dialog.dart` — that the header rewrite has removed every one of the following textual patterns: `AppIcons.calendar`, `AppIcons.close`, `AppTextStyles.title3`, `context.colors.background` (as the close-button background — verify remaining hits, if any, belong to other elements of the file), `${widget.bandName}` (the removed interpolation), `widget.bandName` (the removed reader), and `bandName` (the deleted parameter / field / forward / constructor arg). Do _not_ delete the `AppIcons.error` reference in `_buildError`, the `AppColors.primary` reference in `_buildLoading`'s `AppProgressIndicator`, or the `AppIcons.calendarDays` / `AppIcons.email` / `Icons.apple` references inside the three `_InstructionTile` invocations — those live outside the header and are not this cycle's target.
23. Confirm the drag handle above the removed header block remains in place verbatim (the `Container(margin: EdgeInsets.only(top: 12), width: 40, height: 4, decoration: BoxDecoration(color: context.colors.border, borderRadius: BorderRadius.circular(2)))` widget). Cycle 6 does not touch the drag handle.
24. No new import is required. `IconButton`, `Icons.close`, `IconButton.styleFrom`, `RoundedRectangleBorder`, and `BorderRadius` all live in `package:flutter/material.dart`, which is already imported at line 1 of the file. Confirm by grep that no duplicate `import 'package:flutter/material.dart';` line is added to the file.
25. In [lib/features/calendar/widgets/calendar_subscription_dialog.dart](lib/features/calendar/widgets/calendar_subscription_dialog.dart), inside `_CalendarSubscriptionDialogState._buildBody(context, url)`, relocate the `_CopyButton(...)` invocation. The Cycle 5 shape is: (description `Text`) → `const SizedBox(height: Spacing.space20)` → `_CopyButton(...)` → `const SizedBox(height: Spacing.space20)` → (`'Include in feed:'` label). Cut the `_CopyButton(...)` node together with exactly one of its two surrounding `const SizedBox(height: Spacing.space20)` spacers (pick the `SizedBox` that follows `_CopyButton`, i.e. the one immediately before the `'Include in feed:'` label). Paste the pair (`_CopyButton(...)` then `const SizedBox(height: Spacing.space20)`) between the toggles' trailing `const SizedBox(height: Spacing.space20)` and the `Text('How to subscribe:', ...)` label. After the edit, the widget-tree order inside `_buildBody` reads: description `Text` → `const SizedBox(height: Spacing.space20)` → `'Include in feed:'` label → `const SizedBox(height: Spacing.space8)` → five `AppToggleTile`s (with `const SizedBox(height: Spacing.space4)` between) → `const SizedBox(height: Spacing.space20)` → `_CopyButton(url: url, copied: _copied, onCopied: ...)` → `const SizedBox(height: Spacing.space20)` → `'How to subscribe:'` label → `const SizedBox(height: Spacing.space12)` → three `_InstructionTile`s (with `const SizedBox(height: Spacing.space8)` between) → `const SizedBox(height: Spacing.space20)` → notes `Container`.
26. Confirm the `_CopyButton(...)` invocation is byte-identical after the move: `_CopyButton(url: url, copied: _copied, onCopied: () { setState(() => _copied = true); showSuccessSnackBar(context, message: 'Link copied to clipboard'); Future.delayed(const Duration(seconds: 2), () { if (mounted) setState(() => _copied = false); }); })`. Every argument, closure statement, snackbar message, and duration is preserved verbatim.
27. Confirm the `AppButton(...)` invocation inside `_CopyButton.build()` is byte-identical after the move: `AppButton(label: copied ? 'Copied' : 'Copy subscription link', fullWidth: true, icon: copied ? AppIcons.check : AppIcons.copy, backgroundColor: copied ? context.colors.success : null, onPressed: () { Clipboard.setData(ClipboardData(text: url)); onCopied(); })`. Cycle 6 does not touch this invocation; the label, `fullWidth`, icon, backgroundColor, and onPressed all stay exactly as Cycle 5 set them.
28. Confirm by grep that no other widget inside `_buildBody` moves position. The description `Text`, the "Include in feed:" label, the five `AppToggleTile`s (including their `title:` strings — `Gigs`, `Potential gigs`, `Rehearsals`, `Potential rehearsals`, `Member block-out days`), the "How to subscribe:" label, the three `_InstructionTile` invocations (in order: Apple Calendar, Google Calendar, Outlook), and the notes `Container` all stay in their existing order and with their existing arguments.
29. Do not touch [lib/shared/widgets/toggle_tile.dart](lib/shared/widgets/toggle_tile.dart) — Cycle 3 changes remain in place and are off-limits for Cycle 6.
30. Do not add `AppTextStyles.title3` back as the title style anywhere in this file; do not add a `subtitle` line under the title; do not reintroduce the leading calendar icon; do not change the drag handle; do not move the header inside the scroll body; do not add or remove any other `Padding` / `Container` / `SizedBox` around the header or copy button beyond the ones enumerated in this Cycle 6 task list. Do not reintroduce `${widget.bandName}` (or any other band-derived interpolation) into the header title — the Cycle 6 target is the exact static literal `'Subscribe to Band Calendar'`. Do not retain `bandName` as an unused required parameter on `showCalendarSubscriptionDialog(...)` or `CalendarSubscriptionDialog`; do not rename it to `_bandName` or `unusedBandName` to silence the analyzer; do not default it to `''` or `'Band'` at the receiving end — the correct move is deletion at both sides of the API in the same Engineer commit.
31. Confirm by `git diff` that the two Cycle 1–3 formatting-only hunks (header `Icon` line-wrap and `AppProgressIndicator()` whitespace collapse) — both located inside `_buildLoading` after the header rewrite, not inside the header itself — the four Cycle 4 typography edits, the Cycle 5 URL-container deletion, the Cycle 5 `label:` swap, and the Cycle 5 `fullWidth: true` addition are all present in the working tree after Cycle 6 lands. Cycle 6 must not undo any of them and must not add new formatting-only hunks elsewhere in the file. Also confirm the two callers (`calendar_screen.dart`, `calendar_tab_content.dart`) show exactly the Cycle 6 dead-argument deletions from Tasks 21c and 21d — 2 lines removed each, no other line touched in either file.

### Final gates (run after Cycle 6 edits, on top of Cycle 1–5 state)

32. Run `flutter analyze --no-fatal-infos`. Confirm zero new warnings or errors introduced by this branch's diff (info-level lints on unrelated files are pre-existing baseline and not this branch's responsibility).
33. Run `flutter test`. Confirm the full suite passes.

## Verification Plan

### Tier 1 — pre-deploy, mechanically executable without a running app

1. **`flutter analyze --no-fatal-infos`** — clean of new warnings and errors introduced by this branch's diff. QA compares against `main` baseline analyzer output; only new diagnostics attributable to the two modified files count against this gate.
2. **`flutter test`** — full suite passes. QA runs `flutter test` in the branch and confirms zero regressions vs. `main`. No new test file is required — this bug is presentation-and-widget-swap; there is no branching business logic to unit-test, and there is no existing golden coverage for this sheet to update (a grep of `test/**` for `calendar_subscription_dialog` returned zero matches).
3. **Static diff review — `calendar_subscription_dialog.dart` (Cycles 1–3)**:
   1. Grep `mainAxisMaxRatio: 1.0` in `showCalendarSubscriptionDialog` — exactly one occurrence.
   2. Grep `useSafeArea: true` in `showCalendarSubscriptionDialog` — exactly one occurrence.
   3. Grep `BoxConstraints(` inside `_CalendarSubscriptionDialogState.build` — zero occurrences (the inner cap is fully removed).
   4. Grep `DecoratedBox(` at the shell position inside `_CalendarSubscriptionDialogState.build` — exactly one occurrence. The shell wrapper is now `DecoratedBox`, not `Container`; `use_decorated_box` is clean.
   5. Grep `keyboardHeight` across `_CalendarSubscriptionDialogState` — zero occurrences. The unused local is removed alongside the inner cap. `safeBottom` still appears exactly twice (the local declaration at line 92 or 93 depending on how the diff lands, and the scroll view padding line).
   6. Grep `Material(` inside `_CopyButton` — zero occurrences (only the file-level outer `Material(color: Colors.transparent)` at the sheet root remains).
   7. Grep `InkWell(` inside `_CopyButton` — zero occurrences.
   8. Grep `AppButton(` inside `_CopyButton` — exactly one occurrence.
   9. Grep `AppButtonVariant.primary` inside `_CopyButton` — zero occurrences. `variant:` is `AppButton`'s default and must be omitted to satisfy `avoid_redundant_argument_values`.
   10. Grep `import '.*/app_button.dart';` in the import block — exactly one occurrence.
4. **Static diff review — `toggle_tile.dart` (Cycle 3)**:
   1. Grep `Switch.adaptive` — zero occurrences.
   2. Grep `AppSwitch(` — exactly one occurrence inside `AppToggleTile.build`.
   3. Grep `import '.*/app_switch.dart';` — exactly one occurrence.
   4. If `AppColors` no longer appears in the file, `brand_colors.dart` import is removed; otherwise it stays. Either outcome is acceptable.
5. **Static diff review — `calendar_subscription_dialog.dart` (Cycle 4 typography)**:
   1. Grep `AppFontSizes.body` inside `_InstructionTile` — exactly one occurrence (the title `Text` at what was line 491).
   2. Grep `AppFontSizes.subhead` inside `_InstructionTile` — exactly one occurrence (the instruction `Text` at what was line 499).
   3. Grep `AppFontSizes.subhead` inside `_NoteBullet` — exactly two occurrences (the bullet-character `Text` and the body `Text`).
   4. Grep `AppFontSizes.caption` inside `_InstructionTile` — zero occurrences.
   5. Grep `AppFontSizes.caption` inside `_NoteBullet` — zero occurrences.
   6. Grep `AppFontSizes.caption` across the entire file — after Cycle 5 lands, **zero** occurrences (Cycle 4 alone would leave exactly one at the URL row; Cycle 5 removes that row and its caption reference alongside it — the two cycles taken together drive the count to zero). QA measures against the branch head after Cycle 5, so zero is the target.
   7. Grep any hardcoded `fontSize: 14` or `fontSize: 16` numeric literal across the entire file — zero occurrences. Tokens only.
   8. Grep `_InstructionTile` and `_NoteBullet` outside `calendar_subscription_dialog.dart` — zero occurrences in `lib/**` (confirms both remain private single-use).
   9. Confirm the two Cycle 1–3 formatting-only hunks (header `Icon` line-wrap and `AppProgressIndicator()` whitespace collapse) are present in the diff versus `main`. Cycles 4 and 5 must not undo them and must not add new formatting-only hunks in other locations.
6. **Static diff review — `calendar_subscription_dialog.dart` (Cycle 5 URL-hide + full-width button)**:
   1. Grep `// Subscription URL` across the entire file — zero occurrences. The comment header for the removed block is gone.
   2. Grep `Expanded(` across the entire file — **exactly three** occurrences against the branch head after Cycle 6: (i) the new `Expanded(child: Text('Subscribe to Band Calendar', ...))` inside the header `Row` of `_CalendarSubscriptionDialogState.build()` (added by Cycle 6), (ii) the pre-existing `Expanded(child: Column(...))` inside `_InstructionTile.build()` that wraps the calendar-name label + platform-specific subtext column (untouched by any cycle in this branch), and (iii) the pre-existing `Expanded(child: Text(text, ...))` inside `_NoteBullet.build()` that wraps the note body next to the bullet glyph (untouched by any cycle in this branch). The Cycle 5 URL-row `Expanded(Text(url, ...))` is gone — its removal is cross-verified by checks 6.3 and 6.4 below (zero `TextOverflow.ellipsis`, zero `fontFamily: 'monospace'`), both of which were unique to the removed URL `Text`. QA measures against the branch head after Cycle 6.
   3. Grep `TextOverflow.ellipsis` across the entire file — zero occurrences. The ellipsis clamp was tied to the removed URL `Text`; nothing else in this file used it.
   4. Grep `fontFamily: 'monospace'` across the entire file — zero occurrences. The monospace style was unique to the URL row.
   5. Grep `AppFontSizes.caption` across the entire file — zero occurrences (see Cycle 4 check 5.6 above).
   6. Grep `SelectableText(` across the entire file — zero occurrences. The URL was not substituted with a hidden-but-selectable widget.
   7. Grep `Offstage(` across the entire file — zero occurrences. Same guard as above; the URL must not appear in the widget tree in any hidden form.
   8. Grep `Visibility(` across the entire file — zero occurrences (or, if any pre-existing `Visibility` outside `_buildBody` was already present, it is unchanged and unrelated to the URL).
   9. Grep `'Copy subscription link'` inside `_CopyButton` — exactly one occurrence.
   10. Grep `'Copy'` inside `_CopyButton` — zero occurrences as a standalone label (only `'Copied'` — the 2-second confirmation label — and `'Copy subscription link'` should appear, and neither matches a plain `'Copy'` literal).
   11. Grep `'Copied'` inside `_CopyButton` — exactly one occurrence (the un-touched confirmation state).
   12. Grep `fullWidth: true` inside `_CopyButton` — exactly one occurrence.
   13. Grep `AppButton(` inside `_CopyButton` — still exactly one occurrence (Cycle 3's Forui wrapper stays; Cycle 5 only adds a field and swaps a string; Cycle 6 does not touch this invocation).
   14. Grep `Container(` inside `_buildBody` — after Cycle 5 the count is 1 (the notes-block styled `Container` at the bottom of the body). Cycle 6 does not touch `_buildBody`'s `Container` count — the target stays **exactly one** against branch head.
   15. Grep `Clipboard.setData(ClipboardData(text: url))` across the entire file — exactly one occurrence, inside `_CopyButton.build()`'s `AppButton.onPressed` closure. The copy target and its data source are unchanged.
7. **Static diff review — `calendar_subscription_dialog.dart` (Cycle 6 header parity + body-order reshuffle)**:
   1. Grep `AppTextStyles.title3` across the entire file — zero occurrences. The header title's token-based style is fully replaced by an inline `TextStyle`.
   2. Grep `AppIcons.calendar` across the entire file — after Cycle 6 lands, the header's leading `Icon(AppIcons.calendar, ...)` is gone; grep target is **zero** occurrences of `AppIcons.calendar` (the tile-row uses `AppIcons.calendarDays`, a different token). Verify no unrelated caller was disturbed.
   3. Grep `AppIcons.close` across the entire file — zero occurrences. The header's close icon becomes `Icons.close` (Material glyph) to match Add Event verbatim.
   4. Grep `Icons.close` inside `_CalendarSubscriptionDialogState.build()` — exactly one occurrence (the header close-button `Icon(Icons.close, size: 16, color: Color(0xFFA1A1AA))`).
   5. Grep `IconButton(` inside `_CalendarSubscriptionDialogState.build()` — exactly one occurrence.
   6. Grep `IconButton.styleFrom(` inside `_CalendarSubscriptionDialogState.build()` — exactly one occurrence.
   7. Grep `Color(0xFF1F1F23)` inside `_CalendarSubscriptionDialogState.build()` — exactly one occurrence (the close-button `backgroundColor`).
   8. Grep `Color(0xFFA1A1AA)` inside `_CalendarSubscriptionDialogState.build()` — exactly one occurrence (the close-button icon color).
   9. Grep `Color(0xFFFAFAFA)` inside `_CalendarSubscriptionDialogState.build()` — exactly one occurrence (the header title color).
   10. Grep `CrossAxisAlignment.start` inside `_CalendarSubscriptionDialogState.build()` — the count grows by exactly one relative to the Cycle 5 baseline (the new header `Row` uses it). Any pre-existing `CrossAxisAlignment.start` inside `_buildBody`'s `Column` (`crossAxisAlignment: CrossAxisAlignment.start` on the outer body Column) is unchanged.
   11. Grep `EdgeInsets.fromLTRB(Spacing.pagePadding, 20, Spacing.pagePadding, 16)` across the entire file — exactly one occurrence (the header padding).
   12. Grep `GestureDetector(` inside `_CalendarSubscriptionDialogState.build()` — zero occurrences. The former close-button `GestureDetector` is removed.
   13. Grep `RoundedRectangleBorder(borderRadius: BorderRadius.circular(8))` inside `_CalendarSubscriptionDialogState.build()` — exactly one occurrence (the close-button shape).
   14. Grep `context.colors.background` across the entire file — zero occurrences. The header close-button's former use of this token is replaced by the inline `Color(0xFF1F1F23)`. (If any non-header widget in the file uses `context.colors.background`, this check must be scoped to the header — but a full-file check of the current source returns exactly one occurrence, all inside the former close-button `Container(decoration: BoxDecoration(color: context.colors.background, ...))`.)
   15. Grep `size: 22` across the entire file — zero occurrences. The `size: 22` literal was unique to the removed leading `Icon(AppIcons.calendar, ..., size: 22)`.
   16. Grep `size: 18` inside `_CalendarSubscriptionDialogState.build()` — zero occurrences. The Cycle 5 close-icon size of 18 is replaced by Add Event's size 16.
   17. Grep `BoxShape.circle` across the entire file — zero occurrences. The header close-button's circle shape is replaced by an 8px-radius rounded square.
   18. Grep `size: 16` inside the header — exactly one occurrence (the close-button icon size).
   19. Verify by inspecting `_buildBody`'s `Column.children` order that `_CopyButton(url: url, copied: _copied, onCopied: ...)` now appears _between_ the last `AppToggleTile(title: 'Member block-out days', ...)` and its trailing `SizedBox` and the `Text('How to subscribe:', ...)` label — not between the description paragraph and the "Include in feed:" label. This is a structural reorder check, not a grep check: the reviewer walks `_buildBody`'s child list and confirms the sequence description → space20 → "Include in feed:" → space8 → five toggles → space20 → `_CopyButton` → space20 → "How to subscribe:" → space12 → three instruction tiles → space20 → notes `Container`.
   20. Grep `_CopyButton(` across the entire file — exactly two occurrences total (one class definition of `_CopyButton extends StatelessWidget` and one call site inside `_buildBody`). The call-site count is unchanged from Cycle 5; only its ordinal position in `Column.children` changes.
   21. Confirm by `git diff` versus `main` that Cycle 6 introduces no new `Padding(EdgeInsets.symmetric(horizontal: Spacing.pagePadding))` block (the horizontal padding on the header row is now folded into `EdgeInsets.fromLTRB(Spacing.pagePadding, 20, Spacing.pagePadding, 16)` — check the diff shows that fold, not two padding wrappers). And confirm no new import line was added — `IconButton`, `Icons.close`, `IconButton.styleFrom`, `RoundedRectangleBorder`, `BorderRadius` all come from the already-imported `package:flutter/material.dart`.
   22. Grep `'Subscribe to Band Calendar'` across the entire file — exactly one occurrence (the header title's first `Text` argument). The literal must be byte-exact — no leading/trailing whitespace, no smart quotes, no substring difference.
   23. Grep `'Subscribe to \$\{widget.bandName\} Calendar'` (or any variant of `Subscribe to ...bandName... Calendar` template interpolation) across the entire file — zero occurrences. The interpolated title is fully removed.
   24. Grep `\$\{widget.bandName\}` across the entire file — zero occurrences.
   25. Grep `widget.bandName` across the entire file — zero occurrences.
   26. Grep `bandName` across the entire file — zero occurrences. All four `bandName` references (function parameter, forwarded call, widget field, constructor arg) plus the sole `widget.bandName` reader are gone.
   27. Grep the function signature line — `void showCalendarSubscriptionDialog(BuildContext context, WidgetRef ref, {` should be followed by exactly `required String bandId,` and the closing `}) {` — no other named parameter. Concretely: grep `required String` inside `showCalendarSubscriptionDialog(...)` — exactly one occurrence.
   28. Grep the class field declarations — inside `class CalendarSubscriptionDialog extends ConsumerStatefulWidget {` block, `final String` should appear exactly once (`final String bandId;`) — no other field.
   29. Grep the constructor argument list — inside `const CalendarSubscriptionDialog({` block, `required this.` should appear exactly once (`required this.bandId,`) — no other required constructor argument.
8. **Static diff review — `calendar_screen.dart` and `calendar_tab_content.dart` (Cycle 6 dead-argument removal)**:
   1. Grep `bandName` inside the `GestureDetector` `onTap` closure at [calendar_screen.dart:507-535](lib/features/calendar/calendar_screen.dart#L507-L535) — zero occurrences. Both the local `final bandName = ...;` variable declaration and the `bandName: bandName,` named argument are gone.
   2. Grep `bandName` inside the `GestureDetector` `onTap` closure at [calendar_tab_content.dart:483-511](lib/features/calendar/calendar_tab_content.dart#L483-L511) — zero occurrences. Both the local `final bandName = ...;` variable declaration and the `bandName: bandName,` named argument are gone.
   3. Grep `showCalendarSubscriptionDialog(` across `lib/**` — exactly two call-site occurrences (unchanged from Cycle 5). Each call-site invocation reads `showCalendarSubscriptionDialog(context, ref, bandId: bandId)` — `bandId:` is the only named argument at both sites.
   4. Confirm by `git diff` that neither caller file has any other line touched — the `Center`, outer `GestureDetector`, `Padding`, `Text('+ Subscribe to Calendar', ...)`, `AppColors.primary`, `AppFontSizes.subhead`, `FontWeight.w500`, and every other surrounding widget stay verbatim.
   5. Grep `bandName` elsewhere in either caller file — any remaining `bandName` occurrences must live outside the Subscribe Calendar `onTap` closure (both files use `bandName` for other purposes, e.g. `home_app_bar` composition, band-header labels — those references are outside Cycle 6's scope and must not be touched).
9. **Change Budget check** — measured net line deltas match this plan's stated targets: `calendar_subscription_dialog.dart` net ≈ **−31** (Cycles 1–3) + ≈ **0** (Cycle 4) + ≈ **−16** (Cycle 5) + ≈ **−1** ± 5 (Cycle 6), cumulative ≈ **−58** ± 5 lines against `main`; `toggle_tile.dart` net ≈ **−3** (Cycle 3) with **0** delta added by Cycles 4, 5, or 6; `calendar_screen.dart` and `calendar_tab_content.dart` net ≈ **−2** each (Cycle 6). No file outside the four listed in "Files to Modify" is touched.
10. **`show*` call-site parity** — grep `showCalendarSubscriptionDialog\(` under `lib/` returns exactly the two prior call sites (`calendar_screen.dart`, `calendar_tab_content.dart`); no new call site added, no existing call site removed. Each invocation now passes only `bandId:` as a named argument.

### Owner-run punch list (Tony, at PR-test / apply time — QA hands this to Tony verbatim; QA does not attempt it)

QA cannot launch, build, or drive a running instance of the app. The following require a running instance and are Tony's to run:

1. Launch the app on iOS device or simulator. Sign in to a band. Open the Calendar tab. Scroll to the `+ Subscribe to Calendar` link and tap it. **Expected:** The bottom sheet opens and extends to full available height (top edge sits just under the status bar / Dynamic Island; bottom edge sits at the home-indicator inset). The description paragraph, the full-width `Copy subscription link` button (with no visible URL above or beside it), all five feed-content toggles, all three "How to subscribe" instruction tiles, and the notes block are all visible with normal scrolling behavior; the sheet is not clipped to ~56% of screen height.
2. In the same open sheet, confirm the raw subscription URL is _not_ visible anywhere in the sheet body (no monospace string, no bordered URL container, no truncated ellipsis). Tap the `Copy subscription link` button. **Expected:** The button spans the sheet body's full content width (matching the `Done` button's width in the sheet footer below); the label swaps to `Copied`; the icon swaps to a check; the background color shifts to the app's success green; and a success snackbar reads `Link copied to clipboard`. After ~2 seconds the button reverts to `Copy subscription link` / copy icon / rose background. Paste from the system clipboard into any text field — the pasted URL should be the expected `https://…/functions/v1/calendar-feed?band=<bandId>&token=<token>` ICS URL for the active band (verify against the same band's subscription record in Supabase Studio if needed; the URL is no longer on-screen in the sheet, so this is an end-to-end clipboard round-trip check).
3. In the same open sheet, toggle each of the five switches (`Gigs`, `Potential gigs`, `Rehearsals`, `Potential rehearsals`, `Member block-out days`) once on, then once off. **Expected:** Every toggle responds immediately (no lag), renders as `FSwitch` (soft-rose selected track, `AppColors.switchTrackOff` off track, white thumb — visually identical to `FSwitch` in other sheets like `notification_settings_screen.dart` or `settings_screen.dart`), and each change persists across a sheet close-and-reopen cycle.
4. Tap `Done`. **Expected:** Sheet dismisses, no error snackbar, no visual glitch.
5. Repeat step 1 on Android (physical device or emulator). Confirm sheet reaches full available height above the Android nav bar / gesture handle; the `Done` button is visible and not clipped by the nav bar; the `Copy subscription link` button is full-width and reads without wrapping to two lines at typical Android widths.
6. Repeat step 1 on macOS. Confirm sheet reaches full available height in the desktop window; the full-width `Copy subscription link` button spans the sheet body's content width at desktop widths.
7. Repeat step 1 in the Chrome web build (`flutter run -d chrome`). Confirm sheet reaches full available height in the browser viewport; the full-width `Copy subscription link` button spans the sheet body's content width at typical browser widths, and clicking it copies the URL to the browser's system clipboard (paste into any text field to confirm).
8. Open the sheet from the Calendar tab AND from the Calendar screen (both call sites exist). Confirm identical behavior from both entry points — same full-height, same hidden-URL surface, same full-width copy button, same toggles.
9. Compare visual side-by-side with a sibling calendar sheet (`Add Block Out` or `Day Detail`) opened from the same session. Confirm the drag handle, rounded-top shell, and outer sheet chrome are visually identical — the fix must not drift this sheet's shell away from its siblings. **Note (Cycle 6):** the close-X control is now expected to diverge from `Add Block Out` / `Day Detail` — it will match Add Event's `IconButton` pattern instead of the calendar-feature `GestureDetector` + `Container(shape: circle)` pattern. This is a per-sheet visual choice Tony explicitly approved for Cycle 6; the sibling drift is scoped to just the close-X pill, not the shell.
10. **Cycle 4 typography check.** Scroll to the "How to subscribe" section. **Expected:** "Apple Calendar", "Google Calendar", and "Outlook" labels render at 16px (visibly the same size as the body copy in the sheet's opening paragraph, `"Add your BandRoadie events to your favorite calendar app…"`, which is `AppFontSizes.subhead` = 14 — the labels should read one tier _larger_ than that paragraph); the platform-specific subtext lines beneath each label render at 14px (visibly matching that opening paragraph); the three notes bullets at the bottom (`Calendar is read-only`, `Updates sync automatically`, `Sync timing varies by app (usually 15min - 24hrs)`) render at 14px, matching the subtext lines. No numeric font-size regressions elsewhere in the sheet (section labels still `AppFontSizes.subhead` with `FontWeight.w600`; header title renders at 20px via the Cycle 6 inline `TextStyle`, replacing the Cycle 3 `AppTextStyles.title3` reference). The Cycle 4 owner-run comparison to "the 13px URL string" is superseded by Cycle 5 — the URL is no longer rendered, so the 14px subtext lines and the 14px notes bullets should be visually compared against the 14px opening paragraph, not the removed URL row.
11. **Cycle 5 accessibility check.** Turn on the OS screen reader (VoiceOver on iOS/macOS, TalkBack on Android, ChromeVox on Web). Focus the copy button. **Expected:** The reader announces the button role and reads `Copy subscription link` (in the un-copied state) or `Copied` (in the 2-second confirmation state); no reader announces the URL string as sighted text, because it is no longer in the widget tree. Tap the button under the reader — the `Link copied to clipboard` snackbar text is announced by the platform snackbar's default live region behavior (unchanged from the pre-Cycle-5 flow).
12. **Cycle 6 header parity check.** With the sheet open on any platform, open a second sheet in a parallel session (or take a screenshot for side-by-side comparison) that displays the Add Event drawer (from any Calendar screen, tap `+ Event`). **Expected:** the Subscribe Calendar header title reads _exactly_ `Subscribe to Band Calendar` — no active-band name interpolation, no substring built from the currently-selected band. Regardless of which band Tony has active (or if he switches bands mid-session and reopens the sheet), the title literal never changes. The title and the Add Event header title render at the same font size (20px), same font weight (w600), same color (`Color(0xFFFAFAFA)` — near-white on dark background), and same left-anchored position (no leading icon in either). The close buttons on both sheets render at the same 32×32 pixel size, both use an 8px-radius rounded-square background of `Color(0xFF1F1F23)` (a mid-charcoal — visibly darker than the sheet surface but lighter than the sheet background), and both display the Material `Icons.close` glyph at 16px in `Color(0xFFA1A1AA)` (mid-gray). The vertical padding above the title inside the Subscribe Calendar header (~20px from the drag-handle bottom to the title top) and below the title (~16px from the title bottom to the top of the description paragraph) should read as a comfortable 20/16 rhythm. The Subscribe Calendar drag handle above the header remains visible; Add Event does not have one (Add Event uses `showModalBottomSheet` and Subscribe Calendar uses `showAppBottomSheet` / Forui `showFSheet`, and each family's chrome differs by design — this is not a regression).
12b. **Cycle 6 title-literal cross-band check.** With the app on any platform, open the Subscribe Calendar sheet from one band. Note the title reads `Subscribe to Band Calendar`. Close the sheet, switch to a second band via the band switcher, and reopen the sheet. **Expected:** the title still reads `Subscribe to Band Calendar` verbatim — no band-name substring, no band-name interpolation, no title change of any kind between bands. If either band's name appears anywhere in the title, the check fails. (Sub-verification: open the sheet with no active band by any path that reaches it — historically both callers guard with `if (bandId != null)`, so this path may be unreachable from UI, but if reached the title is still `Subscribe to Band Calendar`.)
13. **Cycle 6 close-button interaction check.** Tap the Subscribe Calendar close button — the sheet dismisses. Focus the close button with the OS screen reader — **Expected:** it announces "Close, button" (VoiceOver / TalkBack / ChromeVox — this is the `IconButton` default semantic; the prior `GestureDetector` did not emit a button role). Focus it with a hardware keyboard on macOS or Web — **Expected:** Tab reaches the close button; Enter/Space triggers the dismiss (`IconButton`'s built-in keyboard activation). The prior `GestureDetector` supported none of these; this cycle upgrades accessibility.
14. **Cycle 6 body-order check.** Scroll the Subscribe Calendar sheet's body from top to bottom. **Expected:** the reading order is (a) description paragraph, then (b) the "Include in feed:" label followed by the five feed toggles, then (c) the full-width `Copy subscription link` button, then (d) the "How to subscribe:" label followed by the three instruction tiles, then (e) the notes block at the bottom. The copy button now sits _between_ the toggles and the how-to steps — a mid-body punctuation — matching Tony's Cycle 6 target. Copy behavior is unchanged: tapping the button copies the URL to the clipboard, swaps to `Copied` for ~2 seconds, and fires the `Link copied to clipboard` snackbar. Toggling any of the five feed switches still updates the preference persistently; no side effect from the button's relocation.

### Tier 2 — post-deploy

n/a — no new SQL objects, no new RPC functions, no new grants, no new edge-function endpoints, no new migrations. There is nothing to run post-apply.

## QA Regression Areas

- Both call sites of `showCalendarSubscriptionDialog` (`calendar_screen.dart` and `calendar_tab_content.dart`) — sheet opens, all content visible, no navigation regression.
- Every other calendar sheet (`add_block_out_drawer.dart`, `day_detail_bottom_sheet.dart`, `view_block_out_drawer.dart`) — the drag handle and rounded-top shell still visually identical to the Subscribe sheet. The close-X control is now expected to diverge (Cycle 6 aligns it with Add Event's `IconButton` pattern instead of the calendar-feature `GestureDetector` + `Container(shape: circle)` pattern). Sibling calendar sheets themselves are not modified.
- Every `AppSwitch` call site listed in `unified-forui-switch-styling` (14 files across settings, contacts, events, financials, members, notifications, setlists, and one_calendar_settings_screen) — appearance unchanged (this branch does not touch `AppSwitch` itself, only makes `AppToggleTile` route through it).
- Every `AppButton` call site (~200+ across the app) — appearance unchanged (this branch does not touch `AppButton` itself, only makes `_CopyButton` route through it).
- Every remaining `Switch.adaptive` call site in the app — must remain unchanged. `AppToggleTile`'s migration to `AppSwitch` does not implicitly migrate any other `Switch.adaptive` call site.
- `AppToggleTile` API surface — `title`, `subtitle`, `value`, `onChanged`, `enabled`, `compact` behave identically for any future caller.
- Calendar tab and Calendar screen — unrelated tabs/screens unchanged.
- Auth (magic-link PKCE), band switching (`activeBandProvider`), ICS feed generation edge function, and RLS on `calendar_feed_preferences` / `band_calendar_tokens` — all unaffected.
- **Cycle 4:** Every other consumer of `AppFontSizes.body` (~40 files) and `AppFontSizes.subhead` (~60 files) is untouched by this cycle; both tokens' numeric values (16 and 14) are unchanged in `design_tokens.dart`.
- **Cycle 4:** The Cycle 4 note that the URL row at line 280 remains the file's last `AppFontSizes.caption` reference is superseded by Cycle 5 — Cycle 5 removes that row, and QA verifies the file has zero `AppFontSizes.caption` references after the branch head is measured. This is not a Cycle 4 regression; it is the expected side effect of the Cycle 5 URL-hide.
- **Cycle 5:** Every other `AppButton.fullWidth: true` call site (~23 files across auth, bands, events, notifications, profile, setlists, and shared components) is untouched; `AppButton.fullWidth`'s implementation at [app_button.dart:161-164](lib/components/ui/app_button.dart#L161-L164) is unchanged.
- **Cycle 5:** Every other `Clipboard.setData(ClipboardData(text: ...))` call site in the app is unchanged; only the button that _triggers_ the copy inside this dialog is touched, not the copy mechanism itself.
- **Cycle 5:** The `calendarBandSubscriptionUrlProvider`, `calendarSubscriptionServiceProvider`, `CalendarFeedPreferences` model, `getBandSubscriptionPreferences`, `updateBandSubscriptionPreferences`, and the ICS-feed edge function are all unchanged. The URL is still generated and delivered exactly as before — it just no longer renders on screen inside the sheet.
- **Cycle 5:** The Copy-button icon (`AppIcons.check` / `AppIcons.copy`), the success-color transition, the 2-second reset timer, the `_copied` state flag on `_CalendarSubscriptionDialogState`, and the `showSuccessSnackBar(context, message: 'Link copied to clipboard')` call are all preserved verbatim.
- **Cycle 6:** The Add Event drawer (`event_editor_drawer.dart`) is the visual reference source but is _not_ modified — its `_buildStickyHeader` widget tree, its `TextStyle`, its close-`IconButton`, and its `EdgeInsets.only(top: 20, bottom: 16)` padding are all unchanged. Nothing in the events feature is touched by this branch. Cross-check: every other consumer of Add Event's header (any parent that composes `_buildStickyHeader` indirectly) is unaffected.
- **Cycle 6:** The three other `IconButton.styleFrom(backgroundColor: Color(0xFF1F1F23))` call sites in the app (`venue_detail_screen.dart:131`, `add_financial_entry_bottom_sheet.dart:699`, `view_gig_drawer.dart:386`) are not modified. Cycle 6 adds a fourth call site with the same hardcoded hex — the pattern's spread is desired here, not accidental.
- **Cycle 6:** Every other `AppTextStyles.title3` call site (~15 files across bands, calendar, financials, members, notifications, setlists) is untouched. The token's definition at [design_tokens.dart:348-355](lib/app/theme/design_tokens.dart#L348-L355) is unchanged (`Geist / 20 / w600 / height 1.25`). Cycle 6 only inlines the equivalent style at one specific call site — the Subscribe Calendar header — because Add Event's inline literal omits `height: 1.25`, which produces a ~2.5px baseline shift on a single-line title inside a `CrossAxisAlignment.start` `Row`.
- **Cycle 6:** Every other `AppIcons.calendar` call site (~6 files across calendar tab labels, home screen, settings) is untouched. Only the Subscribe Calendar header's decorative leading `AppIcons.calendar` is removed — not the icon token itself.
- **Cycle 6:** Every other `context.colors.background` call site in the app is unchanged. Only the Subscribe Calendar header close-button's former use of the token is replaced by the inline `Color(0xFF1F1F23)`.
- **Cycle 6:** The sheet's drag handle, `DecoratedBox` shell, `Flexible + SingleChildScrollView` split, `SheetFooter` Done button, keyboard-inset handling, and all five `AppToggleTile` invocations (including their `title:` strings, `value:` bindings, `onChanged:` closures, and `compact:` flag) are all preserved verbatim. The `_InstructionTile` and `_NoteBullet` widgets and their four Cycle 4 typography swaps are preserved verbatim. Description paragraph and notes-block text are unchanged.
- **Cycle 6:** The `Navigator.of(context).pop()` close action is preserved — the swap is `GestureDetector.onTap: pop` → `IconButton.onPressed: pop`. No routing or navigation change.
- **Cycle 6:** The dialog's `bandId` parameter, its field, and its downstream consumers (`calendarBandSubscriptionUrlProvider(widget.bandId)`, `service.getBandSubscriptionPreferences(widget.bandId)`, `service.updateBandSubscriptionPreferences(widget.bandId, updated)`) are all preserved verbatim. Only `bandName` is deleted; the band-scoping data path is untouched.
- **Cycle 6:** Both callers (`calendar_screen.dart`, `calendar_tab_content.dart`) still compute `activeBand?.id` (or `bandState.activeBand?.id`) locally and gate the sheet open on `if (bandId != null)`. That gate is unchanged; only the local `bandName` computation and the `bandName: bandName,` argument are removed.
- **Cycle 6:** The two callers' other uses of `bandName` (if any exist outside the Subscribe Calendar `onTap` closure — e.g., other `on*` callbacks, header composition, action-button labels) are entirely untouched. Grep of `bandName` in each caller file returns the same non-Subscribe hits before and after Cycle 6.
- **Cycle 6:** Zero external consumers of `showCalendarSubscriptionDialog(...)` or `CalendarSubscriptionDialog(...)` exist (grep across `**/*.dart` including `test/**`, `bandroadie_fresh/**`, `marketing/**` returns only the three internal sites). Removing `bandName` is safe as a module-local expansion.

## Rollout Strategy

Standard PR against `main` from `bug/subscribe-calendar-full-height-forui`. No feature flag, no database migration, no config change, no dependency change. Rollback is a two-file revert.

## Out of Scope

- Any change to `_InstructionTile` or `_NoteBullet` beyond the four in-place `AppFontSizes.caption` → `AppFontSizes.body` / `AppFontSizes.subhead` field swaps enumerated in Cycle 4. The widget constructors, fields, `Icon` sizes and colors, `Column` / `Row` layout, `SizedBox` spacing, `Container` decoration, text colors, and font weights are all preserved verbatim. The "How to subscribe" copy strings and the notes bullet strings are unchanged.
- **Cycle 5:** Any change to `_CopyButton`'s constructor, fields (`url`, `copied`, `onCopied`), `icon:` field, `backgroundColor:` field, or `onPressed:` closure. Cycle 5 only swaps a string literal (label) and adds a boolean field (`fullWidth: true`) inside the existing `AppButton(...)` invocation.
- **Cycle 5:** Any change to the copied-state label (`'Copied'`). Only the un-copied label changes.
- **Cycle 5:** Any change to the description paragraph above the removed URL container, the "Include in feed:" label, the five `AppToggleTile` invocations, the "How to subscribe:" label, the three `_InstructionTile` invocations, the notes `Container`, the `SheetFooter`, or any spacing between them.
- **Cycle 5:** Substituting an alternative "hidden URL" widget for the removed `Expanded(Text(url, …))` (`Offstage`, `Visibility(visible: false)`, `Opacity(opacity: 0)`, `SelectableText`, or any similar construct). The URL must not appear in the widget tree at all.
- **Cycle 5:** Any change to the description of the copy affordance in `snackbar_helper.dart` or the snackbar text `'Link copied to clipboard'`.
- Any change to `showAppBottomSheet` shared wrapper defaults.
- Any change to `AppButton`, `AppSwitch`, `AppProgressIndicator`, or `SheetFooter` shared wrappers.
- Any change to the sheet's shell decoration or drag handle. The header row and close-X are re-styled in Cycle 6 to match Add Event; further changes to them beyond Cycle 6's enumerated edits are out of scope.
- Any refactor of `AppToggleTile`'s outer `Container` styling, `compact` padding logic, subtitle rendering, or public API.
- Migration of any other `Switch.adaptive` call site elsewhere in the app.
- Migration of any other hand-built button elsewhere in the app.
- Migration of `MediaQuery.of(context)` to `MediaQuery.viewInsetsOf(context)` / `viewPaddingOf(context)` in this file (systemic concern tracked separately by `bulk-entry-instructions-cutoff-ios`).
- Any change to `calendar_subscription_service.dart`, `CalendarFeedPreferences`, the URL provider, the preferences RPC, or the ICS feed edge function.
- Golden tests, screenshot tests, or new widget tests — presentation-only change with no branching business logic.
- **Cycle 6:** Any change to Add Event's own `_buildStickyHeader` in `event_editor_drawer.dart`. Add Event is the visual reference source, not a target. Its widget tree, `TextStyle`, `IconButton` invocation, and `Padding` values are all left verbatim.
- **Cycle 6:** Any change to `event_editor_theme.dart`, `kEdCardBorder`, or the events feature's dedicated theme. Subscribe Calendar hardcodes the same `Color(0xFF1F1F23)` and `Color(0xFFA1A1AA)` hex literals inline; it does not import or reference the events-feature theme constants.
- **Cycle 6:** Any change to `AppTextStyles.title3`, `AppFontSizes`, `BrandColors`, or any theme-token file. Cycle 6 inlines an equivalent `TextStyle` at one call site; tokens themselves are unchanged.
- **Cycle 6:** Extraction of the Add-Event-style close pill into a shared `AppSheetCloseButton` widget. If the pattern spreads beyond its current four sites, extraction belongs in a separate architect cycle that surveys every consumer and defines a stable parameter surface.
- **Cycle 6:** Adding a subtitle line under the Subscribe Calendar title, adding a leading icon back, moving the header inside the scroll body, removing the drag handle, or otherwise deviating from the header widget-tree structure enumerated in the Cycle 6 fix (§6 above).
- **Cycle 6:** Adding new `Semantics(...)` wrappers around the header or close button. `IconButton` provides button-role semantics natively; no extra wrapper is required. If a future a11y audit calls for a semantic `label:` argument on `IconButton`, that is a separate cycle with app-wide scope.
- **Cycle 6:** Reflowing any body-order beyond moving the single `_CopyButton` node (plus one spacer) from its Cycle 5 position to its Cycle 6 target position. The description paragraph, "Include in feed:" label, five `AppToggleTile`s, "How to subscribe:" label, three `_InstructionTile`s, and notes `Container` all stay in their pre-Cycle-6 relative order with their pre-Cycle-6 arguments.
- **Cycle 6:** Any change to the title literal beyond swapping `'Subscribe to ${widget.bandName} Calendar'` to the exact static string `'Subscribe to Band Calendar'`. Alternative literals (`'Subscribe'`, `'Calendar Subscription'`, `'Band Calendar Subscription'`, `'Subscribe to Calendar'`), keeping the interpolation with a hard-coded `'Band'` caller argument, or reintroducing band-name dynamism in any other form are all out of scope.
- **Cycle 6:** Preserving `bandName` as a required parameter on `showCalendarSubscriptionDialog(...)` or as a field on `CalendarSubscriptionDialog` for compatibility with any hypothetical external consumer. Grep across `**/*.dart` confirms zero external consumers exist. Compatibility with a caller that does not exist is out of scope; the correct move for a dead required parameter is deletion.
- **Cycle 6:** Renaming `bandName` to `_bandName`, `unusedBandName`, or any other underscore-prefixed identifier to silence the analyzer while retaining the parameter surface. That hides the deadness rather than removing it and is out of scope.
- **Cycle 6:** Defaulting `bandName` to `''`, `'Band'`, or any other literal at the receiving end and continuing to accept the named argument at the call sites. Deletion, not defaulting, is the plan; defaulting is out of scope.
- **Cycle 6:** Any change to the two callers (`calendar_screen.dart`, `calendar_tab_content.dart`) beyond the two-line dead-argument removal enumerated in Tasks 21c and 21d. Every other line, widget, provider read, `?? 'Band'` fallback for other purposes, action button, `Center`, `Padding`, `Text`, and surrounding closure body stays verbatim.
